# Surreal integration documentation patch report

<!-- Generated by: Codex | Date: 2026-09-12 | Rev: 1 | Platform: Codex / win32 -->

<!-- Updated by: Codex | Date: 2026-09-12 | Rev: 2 | Platform: Codex / win32 | Changes: record live proof continuation | Context: new results supersede original partial status -->

Continuation: added LIVE-GRAPH-PROOF and ATOMIC-EOD-HANDOFF; linked actual25-row
historical load, null-preserving replay, populated restore and exact33-table parity
from runtime/deployment receipts and TODO. Updated runbook to install the new
dependency-order helper and preserve original exports/enforcement. Full-corpus,
live-census, automatic pipeline, alerts and scale gates deliberately remain open.
Documented the tested graph-project-catalog CLI and final97-test run; clarified
that the proof sample's selection never becomes a migration omission rule and
that any historical hash bridge belongs to the coordinating task, not this graph.

Updated the deployment receipt to replace stale credential, hostname and backup
pending claims with verified results. Updated the runtime receipt with the scoped
credential boundary, released SDK fixes, commands/endpoints and reported tests.
Updated MASTER-TODO deployment and deployment-choice hold items only; left broad
graph schema, projection, expansion and rebuild gates unchecked because partial
implementation does not meet their full criteria. Existing MASTER-TODO byline and
owner decision are preserved; this receipt records this additional author's edits.

Deliberately not changed: historical crash-recovery chronology, unrelated dirty
documentation, corpus migration status, evidence approval state, and original
source files. Schema-only restore and a configured timer are distinguished from
data-scale recovery and recurring restore drills. All updates are status repair,
not a change to product architecture or source-data authority.

Added SURREAL-TABLE-GUIDE.md explaining all 14 nodes and 19 relations, exact-byte
identity versus separate occurrences, multipart/account/export separation, and
why a graph relationship does not authorize moving/deleting evidence. The guide
describes schema meaning, not corpus-loading completion.

Reconciled the backup runbook and deployment README with the installed/enabled
timer and successful schema restore. Preserved operator installation steps and
the original static-review limits; explicitly left external alerts and recurring
restore drills unconfigured.

Final runtime receipt records the explicit inventory adapter, 81 independently
passing tests, scoped lint, deployed syntax validation and precise read-only
authorization observations. Added progress beneath CBX-P6-007 without marking
the full lake projection/reconciliation gate complete. Preserved real corpus
load and data-scale recovery as unverified next steps.

Continuation after crash recovery: added R2-B2-OCCURRENCE-GRAPH-CONTRACT-V1 and
updated live/runtime/TODO status for the hash-bound migration projection. Recorded
synthetic migration-builder provenance, zero-write replay and 102 passing tests.
Preserved real-generation reconciliation, transfer execution, B2 ownership and
atomic-unit decisions as unfinished external boundaries.

Corrected the earlier messaging limitation after the Codex task-send capability
became available: the atomic and occurrence-map contracts were delivered to the
coordinating task. Updated final verification to 104 tests and documented the new
completion-aware projection API; no transfer authority changed.
