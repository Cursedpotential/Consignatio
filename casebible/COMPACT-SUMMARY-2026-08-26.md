# Compact summaries — 2026-08-26

> _Byline: Claude Code · Fable 5 · auto-generated by PostCompact hook_
> Transient compaction summaries persisted to disk. The newest HANDOFF-*.md,
> when present, remains the authoritative session state.

---

## Compact @ 2026-08-26T20:24:04 (session 0636299a-05c4-460c-b4c0-49dd63d0ddba, trigger manual)

<analysis>
Let me go through this conversation chronologically to capture all technical details, user requests, and decisions.

**Phase 1: Finding the test that was submitted**
The user asked me to find a test they had submitted testing different LLMs with doc classification, saved in PG DB, "maybe ALSO in duckdb/lance local." I explored the E:\AI_Workspace directory structure, found the platform workspace, and found a design doc for a "Classification & Sentiment Analysis Test System." Mid-turn the user interjected asking me to also check "pg18 on ovh-files" and mentioned this test was "done a couple months ago" with results already saved.

**Phase 2: Secrets and connection setup**
User asked me to check ~/.secrets folder. I listed it (names only, per security practices) and found casebible-databases.md which had connection details. Found a "SUPERSEDED IN PART" correction block noting Platform Postgres moved from ovh-data to ovh-files at 100.91.190.107:5432, database `ai`, user `ai`, password in Agno-MCP-Platform.env under DB_PASS key. Also learned PG18.1 is the live version, and the `ai` database has 158 tables, 134 empty (per 2026-08-02 wipe policy "test data must never become canonical").

I installed psycopg via uv, found the right Python interpreter at `C:/Users/matts/AppData/Roaming/uv/python/cpython-3.14.6-windows-x86_64-none/python.exe`, and connected to PG18. Found extensions installed: vector 0.8.6, pg_duckdb 1.1.0, postgis, pg_trgm, etc. Found candidate tables like `analysis.human_label` (1918 rows), `analysis.human_label_gold` (1918 rows), `ops.workflow_run`, etc.

I then listed all databases on the instance: traceiq, ai, ai_test_ingest, temporal, temporal_visibility, postgres. Explored `ai` database's populated tables extensively.

User then asked about data conflation - noted that `ai.casebible_evidence_contents` (24 rows, real ChatGPT export content) and 3 empty test tables (`casebible_evidence_test_contents`, `casebible_ingest_test_contents`, `casebible_ingest_test2_contents`) were sitting inside the Agno platform's `ai` schema, which shouldn't hold Case Bible data.

**Phase 3: New database creation**
User said: "Make a new properly named properly separate database migrate the data that exists that we and let's start fresh. Not in AI make a case Bible in that instance"

I created a new `casebible` database on the same PG18 instance (ovh-files), installed all 11 extensions (vector, pg_trgm, pgcrypto, btree_gin, btree_gist, citext, hstore, ltree, unaccent, fuzzystrmatch, pg_duckdb), created 7 schemas (evidence, analysis, knowledge, reference, working, ops, llm_eval), created `knowledge.evidence_contents` table, migrated the 24 rows via Python script with SHA verification (source and target hashes matched: b8ae3b1db38521b4), then dropped the 4 source tables from `ai` schema after verification.

**Phase 4: Recovery investigation**
User asked "Any chance of recovery somehow" regarding the labels. This triggered an extensive recovery investigation:
- Searched local workspace for backups/dumps
- Found `_db_backups/20260801-pgdump/` and `_db_backups/20260801-pre-case-data-dump/`
- Found `casebible/backups/pg/20260622/casebible.dump`
- Found `casebible/exports/labeling-workbook-imessage-2989.csv` and `.xlsx`

Extensive grep/analysis showed the labels were NEVER filled in - not "lost" but never created:
- CSV/XLSX workbook (Jul 3): your_label column 100% empty
- Pre-wipe backup CSV (Aug 1 15:47): all labels `[]`, all fields blank
- pg_dumpall.sql.gz from recovered volume (Aug 1 20:41): same empty state
- User mentioned "I just attached I think the old block storage I THINK that belonged to ovh-data to ovh-app... see if theres anything there to recover" - I found `/dev/sdb` (100G, label agno-data) on ovh-app, mounted read-only at /mnt/recover
- Found the Aug 1 hot backup, and critically a LIVE PG18 cluster at /mnt/recover/agno/volumes/pgdata/18/docker, last written Aug 3 02:32 - cleanly shut down
- I copied this to /var/tmp/pgrecover, started it on port 55432 with trust auth, confirmed same empty label state (1918 rows, labeled=0 for both tables)
- Formally ruled out casebible.dump via pg_restore -l (only 5 media tables, no analysis schema)
- Found documentary confirmation in LOG.md:324 (2026-07-07) explicitly listing "ground-truth labeling workbook" as STILL OPEN

**Phase 5: PG16 discovery and media migration**
User asked "did you also check the PG16 systems that I think is still running on files" - found TWO PG16 containers on ovh-files:
- `rmj36da884vt5nzueh28mlng` (running, unpublished port) - held `casebible` database, 111MB, with tables: enrichment (15252 rows with LLM classification data - model, confidence, latency_ms), faces, faces_scanned, photos, screenshots
- `casebible-pg` (exited 2026-06-20) - older volume, verified as strict subset, discarded

I found the actual LLM classification run! Model distribution showed: ibm-granite/granite-4.1-8b (8376), gemini-3-flash-preview (5574), mistralai/mistral-small-4-119b-2603 (1019), etc. for enrichment; claude-haiku-4-5, nvidia/nemotron-nano-12b-v2-vl:free, gemini-2.5-flash for screenshots.

Migrated PG16 `casebible` database into PG18 `casebible.media` schema via pg_dump -Fc → docker cp → pg_restore → alter table set schema. Verified exact counts: enrichment 15252, faces 6911, faces_scanned 7121, photos 7121, screenshots 3113.

I then saved two memory files:
- Updated `detection-refinement-ground-truth-labeling.md` with RESOLVED section
- Created `casebible-llm-classification-run-june-2026.md` with full findings
- Updated MEMORY.md index

**Phase 6: Consolidation verification and full migration**
User asked "So all of the case Bible data everywhere that we have found it has now been consolidated in a logical manner inside of one database correct??" - I ran a full audit and reported NO, data was still spread across 3 databases (ai, ai_test_ingest, casebible). Listed all remaining tables in `ai` (analysis.human_label, human_label_gold, court_case, matter, reference.* tables, working.context_record, ai.*_knowledge_contents) and `ai_test_ingest` (evidence.* and working.* SMS-related tables).

I proposed two options: (1) finish DB consolidation, (2) run full source census, flagging that reference.detection_pattern might have live code readers.

User responded: "Yes to the first part however I just realized that the AI test ingest there's probably evidence uh related Now the entire shape changes so I'm just going to update the other project that this is where the test lives now or the previous test information So just Fyi" - I interpreted this as: proceed with `ai` migration, leave `ai_test_ingest` alone (documenting it externally). 

I attempted to do a reader-check grep but user REJECTED both grep tool calls and said: "No go ahead and grab the the test tables also" - meaning include ai_test_ingest tables too, skip the reader check.

I then executed the full migration:
- Used pg_dump/psql inside the PG18 container (b8369e7bcd7b) on ovh2 (ovh-files)
- Hit type-dependency errors: reference tables depend on custom enum types living in `ai` schema (citext, mcl_factor, category_polarity, pattern_match_type, sensitivity_tier) and evidence tables depend on evidence schema enums (acquisition_authority, acquisition_method, record_medium) plus ai schema enums (evidence_tier, precision_class, review_state)
- Created these types in casebible (public schema for ai-schema types, evidence schema for evidence-specific ones)
- Used sed to rewrite `ai.X` type references to `public.X` in dumped SQL, restored in 3 sections (pre-data, data, post-data)
- Fixed a bug where `docker exec` without `-i` doesn't forward stdin (my initial type creation attempt failed silently)
- Migrated 4 trigger functions (evidence.raw_no_mutate, evidence.source_immutable_core, working.derived_write_guard, analysis.set_case_management_updated_at) so evidence tables keep their immutability guards
- Discovered and fixed one dropped table (working.event_source_record data got lost in cascade, re-restored)
- Ran full verification: count + md5 hash of ordered row content for all 24 tables - all matched
- Renamed 3 Agno knowledge tables from ai.*_knowledge_contents to knowledge.*_contents
- Created ops.migration_log table recording all 24 migrations with source/target/count/note
- Confirmed no stray `ai` schema was created in casebible

Final state: casebible = 31 tables / 117MB. All verified. Nothing dropped from source ai or ai_test_ingest (deliberately, pending explicit approval). Some FKs didn't come across (pointing to tables out of scope like ops.processing_run, working.conversation, working.device, working.entity, analysis.timeline_event, evidence.file_node - all empty in source so no violation, but constraint enforcement absent).

I flagged: PG16 source copies (running container + exited casebible-pg volume) still exist, I deliberately didn't drop them post-migration.

**Phase 7: LLM Testing Harness**
User ran /model command (set to Sonnet 5) then asked me to: "create a set of tests that tests each model's functions as far as tool usage summarization maybe even instruction following if you can figure out how design a set of tests and test all of the models that we have available through NIM So you do a really basic test first just doesn't respond yes or no And if it responds then we can do more tests as well as Olama and for that matter open code but for open code filter free models and then even though it's out of scope for the moment design a test for RE rankers and embedders if possible"

I began building this, then user corrected: "Sorry open router not open code" - meaning OpenRouter, not OpenCode.

I set up env.py loading credentials from ~/.secrets/Agno-MCP-Platform.env (NVIDIA_API_KEY, NVIDIA_BASE_URL, NVIDIA_EMBED_TEXT_ID, NVIDIA_EMBED_CODE_ID, NVIDIA_RERANK_ID, OLLAMA_API_KEY, OPENROUTER_API_KEY, GEMINI_API_KEY, etc.)

User added: "Throw Gemini in there for a reference" - added Gemini as 4th provider.

I fetched model lists: NIM 102 models, Ollama Cloud 19 models (via https://ollama.com/v1/models), OpenRouter 417 models. Gemini initial key failed (400 API key not valid).

User then said: "Keep in mind Nvidia Nim is a free API so if we hit it too hard we're going to get limited and Olava Cloud I have a pro account which gives me three concurrent models to one" - critical constraint: NIM needs low concurrency (I used 2), Ollama Cloud capped at 3 concurrent.

User then said: "Multiple models same test figure out what's available" and "Could probably also pull menstrual and cohere in any other Apis you can find in there for shits and giggles" - I correctly interpreted "menstrual" as "Mistral" (found MISTRAL_API_KEY in secrets), searched for Cohere (not found), and also found GROQ_API_KEY, CEREBRAS_API_KEY, OPENAI_API_KEY - added all as "bonus" providers.

I fixed Gemini auth (needed query param `key=` not just Bearer header) - discovered GOOGLE_API_KEY works but GEMINI_API_KEY, GEMINI_API_KEY_4 are dead API keys (400 INVALID), GEMINI_API_KEY_2/_3 are stale OAuth access tokens (401), not valid API keys at all.

User then said: "You should grab all of the Gemini and Google keys there should be four of them but for the other three we don't need to run every single model on every single account just run a really cheap model on the other accounts just to make sure that they're live" - I tested all 5 Gemini/Google key variants against the models endpoint; only GOOGLE_API_KEY was live (50 models).

I picked cheap reference models for bonus providers: Groq (allam-2-7b), Cerebras (gemma-4-31b), OpenAI (gpt-4.1-nano), Google (gemini-2.5-flash-lite), Mistral (mistral-small-latest, initially failed 402 payment required).

I built `tier0.py` - full async liveness sweep script with:
- Per-provider concurrency semaphores (nim=2, ollama_cloud=3, openrouter_free=5, default=3)
- Retry/backoff on 429 (up to 2 retries, 3s*attempt delay)
- Prompt: "Are you operational? Reply with only the single word YES or NO. No punctuation, no explanation."
- Filters OpenRouter models to free tier only (`:free` suffix or pricing prompt/completion both 0)
- Records ok, status, latency_s, error, content, followed_format (whether it replied exactly YES/NO)

Ran tier0.py in background (task bkdahldj5) - 145 total targets (102 NIM + 19 Ollama + 19 OpenRouter free + 5 bonus).

While waiting, I built:
- `tier1.py` - Tier 1 capability tests: tool_use (TOOL_SCHEMA for get_case_status function, checks correct name/valid JSON args/required key), summarization (SUMMARY_SOURCE about a fictional Riverton County custody case, SUMMARY_KEY_FACTS list, checks word count < 50% source AND >=2 key facts - later found to be flawed scoring), instruction_following (5-word lowercase no-punctuation courtroom description, regex-checked)
- `schema.sql` - designed llm_eval.probe_run and llm_eval.probe_result tables
- `tier_embed_rerank.py` - embedder/reranker test design for NIM (text+code embed models, checks cosine similarity ordering and dimension match; rerank test tries two URL shapes)

User interjected: "Mistral is fixed" - confirmed Mistral now has credit (was 402 before)
User interjected: "Google can embed" - I added Google embedding test (gemini-embedding-001 via native embedContent endpoint)
User interjected: "so can mistral" and "and cohear i think" - I extended tier_embed_rerank.py to add embed_google(), embed_mistral(), embed_cohere() (gated on missing COHERE_API_KEY, gracefully skips)

Tier0 completed: 65/145 initially OK, but discovered 29 cases had ok=True but empty content (HTTP 200 but content="" due to reasoning models burning token budget on hidden reasoning). Confirmed via raw inspection of ollama_cloud kimi-k2.6 response showing a "reasoning" field consuming all 20 tokens with finish_reason: "length".

I wrote `tier0_patch.py` to retry these 29 with max_tokens=400, producing tier0_results_final.json. Final count: 62/145 truly responded. Breakdown by provider:
- cerebras: 0/1 (still 402 payment required - dead)
- google: 1/1 (gemini-2.5-flash-lite)
- groq: 1/1 (allam-2-7b)
- mistral: 1/1 (mistral-small-latest, confirmed fixed)
- nim: 28/102 (most 404 failures are stale catalog entries not actually deployed)
- ollama_cloud: 19/19 (100%)
- openai: 1/1 (gpt-4.1-nano)
- openrouter_free: 11/19

I persisted this via `persist.py` to casebible.llm_eval (created schema, run_id=1, 145 rows in probe_result). Fixed a DSN bug where DB_HOST from env pointed to "agentos-db" (docker-internal hostname) - had to hardcode 100.91.190.107:5432.

I reported comprehensive Tier 0 results with the provider breakdown table and asked if user wanted Tier 1 to run.

User responded: "yes"

I fixed tier1.py to read from tier0_results_final.json (not the original tier0_results.json), bumped max_tokens in all 3 probes (tool_use: 200→500, summarization: 150→500, instruction_following: 30→300) to account for reasoning model overhead, then ran tier1.py in background (task bd0nbvevx).

I attempted to block-wait on the task via TaskOutput with block:true, but this was REJECTED by the user via tool rejection, alongside the user's actual message showing a fragment of what appears to be a rendered table from BEFORE (possibly from context that had been compacted/truncated) asking "why only one midel??" about Google having only 1 model tested.

I explained the reasoning (Google/Groq/Cerebras/OpenAI/Mistral were scoped as single reference pings per original ask, not full sweeps like NIM/Ollama/OpenRouter which were explicitly named for full coverage), noted Tier1 background task was still running, then asked via AskUserQuestion whether to expand bonus providers to full sweep. This got no response after 300s timeout.

The Tier 1 background task (bd0nbvevx) then completed via notification. I proceeded using best judgment (leave bonus providers as single reference pings per original scope) and pulled the Tier 1 results.

Tier 1 raw results: tool_use 50/62, summarization 15/62 (very low), instruction_following 34/62.

I investigated the low summarization pass rate and found it was a SCORER BUG, not model failure - my "word count < 50% of source" threshold was too strict for a "summarize in one sentence" task where good summaries naturally run 60-90 words when packing in many facts. Confirmed via inspecting actual good summaries from mistral-small-latest, gpt-4.1-nano, nemotron-3-super-120b that had 5-6/8 key facts but failed only due to length.

I rescored summarization offline using stored content (no new API calls) with relaxed criteria: words < 130 (raw source word count) AND hits >= 3 AND sentence_like (<=2 periods). This raised summarization pass to 44/62 (29 flips).

I then checked instruction_following failures - found these were mostly legitimate (wrong word count, commas instead of spaces, content-safety models declining, translation model echoing prompt) EXCEPT for 11 models that returned None/empty content even at 300 tokens - same reasoning-model token-starvation pattern as Tier 0.

I wrote `tier1_patch.py` to rerun instruction_following at max_tokens=1500 for these 11 targets, reusing functions imported from tier1.py (score_tool_use, score_summary, score_instruction, TOOL_PROMPT, TOOL_SCHEMA, SUMMARY_PROMPT, INSTR_PROMPT, probe, sem_for). This raised instruction_following from 34/62 to 42/62.

Final Tier 1 tallies:
- n=62, tool_use=50, summarization=44, instruction_following=42, all_three_pass=34
- By provider: groq{n:1,tool:0,summ:1,instr:0}, google{n:1,tool:1,summ:1,instr:1}, mistral{n:1,tool:1,summ:1,instr:0}, nim{n:28,tool:18,summ:17,instr:15}, openai{n:1,tool:1,summ:1,instr:1}, openrouter_free{n:11,tool:10,summ:8,instr:7}, ollama_cloud{n:19,tool:19,summ:15,instr:18}
- List of 34 models passing all 3 probes was printed

I created `persist_tier1.py` to persist Tier 1 results into casebible.llm_eval, creating run_id=2 with 186 probe_result rows (62 models × 3 probes: tool_use, summarization, instruction_following). Successfully ran, confirmed: instruction_following 42/62, summarization 44/62, tool_use 50/62.

This is exactly where the conversation ends - I just successfully ran persist_tier1.py and got the confirmation output. I have NOT yet reported this final summary back to the user in a text response - the last tool call was the persist script execution, and the summarization request interrupted before I could compose the report-back message to the user.

Key files created in scratchpad directory `C:\Users\matts\AppData\Local\Temp\claude\E--AI-Workspace-casebible\0636299a-05c4-460c-b4c0-49dd63d0ddba\scratchpad\llm_eval\`:
- env.py - credential loader
- list_models.py, list_models2.py - model enumeration scripts
- gemini_keycheck.py - Gemini/Google key validation
- nim_models.json, ollama_models.json, openrouter_models.json, groq_models.json, cerebras_models.json, openai_models.json, gemini_models_GOOGLE_API_KEY.json, gemini_keycheck.json - model list caches
- tier0.py - liveness sweep script
- tier0_run.log, tier0_results.json - raw tier0 output
- tier0_patch.py, tier0_patch_results.json, tier0_results_final.json - patched/corrected tier0 results
- tier1.py - capability test script (tool_use, summarization, instruction_following)
- tier1_run.log, tier1_results.json - tier1 output (rescored/patched in place)
- tier1_patch.py - patch script for empty instruction_following responses
- schema.sql - llm_eval schema DDL
- persist.py - Tier0 DB persistence script
- persist_tier1.py - Tier1 DB persistence script
- tier_embed_rerank.py - embedder/reranker test design (not yet executed)

I have not yet reported the final Tier 1 results with the scoring corrections to the user, nor addressed the still-open question about whether to expand bonus providers (Google/Groq/Cerebras/OpenAI/Mistral) to full catalog sweeps, nor run the embed/rerank tests (explicitly noted as "out of scope for the moment" by the user but I built the design/script anyway).

Now let me also note critical security/constraint instructions given throughout:
- Never print/echo raw secret values in shell commands or transcripts unfiltered (though per global CLAUDE.md, transcript-only exposure is acceptable, git-tracked files are the hard line) - I have been redacting/truncating key previews when printing (e.g., "SET len=70" rather than full value, or first 6 chars + "...")
- NIM is free tier - avoid hitting it too hard (rate limit risk) - used concurrency=2
- Ollama Cloud pro account allows 3 concurrent models - used concurrency=3
- User confirmed OpenRouter (not OpenCode) for the free-model filter test
- Never delete data without explicit approval - all migrations were copy-first-then-drop-only-after-verification (for the ai schema evidence_contents migration) or copy-only-no-drop (for the larger ai/ai_test_ingest consolidation, pending explicit approval)
- User explicitly said "No go ahead and grab the the test tables also" overriding my planned reader-check gate on reference.detection_pattern etc.

I should make sure to capture all of this in the summary comprehensively, focusing on:
1. The full LLM testing harness build (most recent/current work)
2. The DB consolidation work (major completed task)
3. The recovery investigation (completed, resolved)
4. All user verbatim messages
5. Current state and next steps

Given the summarization instructions have no special additional focus specified other than the standard template, I'll follow the standard 9-section structure exactly.
</analysis>

<summary>
1. Primary Request and Intent:
   - Find a test the user submitted comparing different LLMs on document classification, saved in Postgres and possibly DuckDB/Lance locally (from "a couple months ago").
   - Check ~/.secrets folder for connection details; check PG18 on ovh-files; check PG16 that might still be running on ovh-files.
   - Investigate whether ground-truth human labels for a 1,918-message pilot conversation could be recovered after the user reported they "disappeared."
   - Consolidate all discovered Case Bible data into one properly-named, logically separate database (not inside the Agno platform's `ai` database) — "Make a new properly named properly separate database migrate the data that exists that we and let's start fresh. Not in AI make a case Bible in that instance."
   - Later, finish full consolidation: move ALL remaining Case Bible tables out of `ai` (and, per final instruction, out of `ai_test_ingest` too) into the new `casebible` database, verified by count+hash, without deleting anything from source yet.
   - Design and build a tiered LLM-provider test harness: Tier 0 = simple liveness ("does it respond yes/no"), then if models respond, Tier 1 = tool-usage / summarization / instruction-following tests. Cover NIM (free tier, must throttle), Ollama Cloud (pro account, max 3 concurrent), OpenRouter (filtered to free models only — corrected from an initial "OpenCode" typo). Add Gemini/Google as a reference provider. Add "Mistral" (misheard as "menstrual") and Cohere and any other providers found in secrets (Groq, Cerebras, OpenAI all found and added) "for shits and giggles" — as single cheap reference pings, not full sweeps, for these bonus/reference providers (per explicit clarification: "we don't need to run every single model on every single account just run a really cheap model on the other accounts just to make sure that they're live"). Also design (not necessarily run) embedder and reranker tests, initially "out of scope for the moment," later expanded per user request to include Google and Mistral embedders (and Cohere if a key exists) in addition to NIM.
   - Persist all test results into the `casebible` database (specifically a new `llm_eval` schema) for durability.
   - Run Tier 1 tests after Tier 0 confirmed 62 live models ("yes" to proceeding).

2. Key Technical Concepts:
   - PostgreSQL 18.1 on ovh-files (tailnet IP 100.91.190.107:5432), with pgvector, pg_duckdb, postgis, pg_trgm, citext, and other extensions.
   - Docker-based Postgres deployment; multiple containers can coexist per host including unpublished-port containers invisible to simple port scans.
   - pg_dump/pg_restore section-based restore (`--section=pre-data|data|post-data`) to work around cross-schema custom type dependencies.
   - Custom PostgreSQL enum types (`mcl_factor` = Michigan custody factors a-l, `category_polarity`, `sensitivity_tier`, `pattern_match_type`, `evidence_tier`, `precision_class`, `review_state`, `acquisition_authority`, `acquisition_method`, `record_medium`) that lived in the `ai` schema/`evidence` schema and blocked cross-database migration until recreated in the target DB.
   - `citext` extension needed installing separately per-database.
   - Verification methodology: exact row count PLUS `md5(string_agg(x::text,'|' order by x::text))` row-content hash to prove byte-for-byte migration fidelity.
   - `ops.migration_log` table pattern for recording provenance of migrated data.
   - Async Python HTTP testing harness using `httpx.AsyncClient`, `asyncio.Semaphore` per-provider concurrency control, retry/backoff on HTTP 429.
   - OpenAI-compatible `/chat/completions` and `/embeddings` endpoints used uniformly across NIM, Ollama Cloud, OpenRouter, Groq, Cerebras, OpenAI, Mistral, and Google's OpenAI-compat layer.
   - Google Gemini native API requires `?key=` query param (not just Bearer auth) for the native `/v1beta/models` endpoint; Google also exposes an OpenAI-compatible endpoint.
   - Reasoning models (Kimi, DeepSeek, GLM, some Nemotron variants) emit hidden `<think>`/`reasoning` content that consumes the `max_tokens` budget before producing the final answer, appearing as HTTP 200 with empty `content` and `finish_reason: "length"` — a token-budget/methodology issue, not true liveness failure.
   - NIM's `/v1/models` catalog lists many models that return 404 "Function not found for account" — the catalog is stale relative to what's actually deployable on a given account.
   - OpenRouter free-model filtering: `id.endswith(':free')` OR `pricing.prompt == 0 and pricing.completion == 0`.
   - Provider constraint: NIM is free-tier (rate-limit risk, concurrency capped at 2), Ollama Cloud pro account allows exactly 3 concurrent requests (concurrency capped at 3).
   - `python3` on this Windows machine resolves to a different interpreter (3.13) than the uv-managed one with psycopg installed (`C:/Users/matts/AppData/Roaming/uv/python/cpython-3.14.6-windows-x86_64-none/python.exe`), a recurring environment gotcha.
   - `docker exec` without `-i` flag does not forward stdin — silently no-ops piped heredoc input, which caused a debugging detour.
   - Global CLAUDE.md rules in effect: never `git add -A`, verify-before-claiming, transcript-only secret exposure is acceptable but git-tracked files are the hard line, never delete without explicit approval (move to stale/archive instead), data-moving operations need dry-run/scope-check first.

3. Files and Code Sections:
   - `~/.secrets/Agno-MCP-Platform.env` — source of all provider API keys (NVIDIA_API_KEY, NVIDIA_BASE_URL, NVIDIA_EMBED_TEXT_ID, NVIDIA_EMBED_CODE_ID, NVIDIA_RERANK_ID, OLLAMA_API_KEY, OPENROUTER_API_KEY, GEMINI_API_KEY/_2/_3/_4, GOOGLE_API_KEY, GROQ_API_KEY, CEREBRAS_API_KEY, OPENAI_API_KEY, DB_HOST/DB_PORT/DB_USER/DB_PASS for Postgres). Read-only, never modified.
   - `~/.secrets/mistral.env` — MISTRAL_API_KEY.
   - `~/.secrets/casebible-databases.md` — contained the critical "SUPERSEDED IN PART" correction block giving the current ovh-files PG18 connection details and noting the box-rename history (ovh1→ovh-app, ovh2→ovh-files, ovh3→ovh-data, IONOS→ion-control).
   - Scratchpad directory: `C:\Users\matts\AppData\Local\Temp\claude\E--AI-Workspace-casebible\0636299a-05c4-460c-b4c0-49dd63d0ddba\scratchpad\`
     - `pg_probe.py`, `pg_dbs.py`, `pg_labels.py`, `pg_flag.py`, `pg_flag2.py`, `pg_cb_rows.py`, `pg_conflation.py`, `cb_create.py`, `cb_verify_drop.py`, `consolidation_audit.py` — sequence of ad-hoc PG18 exploration/migration scripts used during Phases 1-3 and 6. `cb_create.py` created the `casebible` database, extensions, schemas, and did the first migration of `knowledge.evidence_contents` (24 rows) with a JSON backup at `casebible_evidence_contents_backup.json`. `cb_verify_drop.py` did SHA-verified comparison then dropped the 4 old tables from `ai`.
     - Under `scratchpad/llm_eval/`:
       - `env.py` — final version loads from `Agno-MCP-Platform.env`, `mistral.env`, `n8n-ovh2.env`, `memsearch.env`; exposes NVIDIA_*, OLLAMA_API_KEY, OPENROUTER_API_KEY, GEMINI_API_KEY(+_2/_3/_4), GOOGLE_API_KEY, GEMINI_KEYS dict, GROQ_API_KEY, MISTRAL_API_KEY, CEREBRAS_API_KEY, OPENAI_API_KEY, COHERE_API_KEY (None — not present), DB_HOST/PORT/USER/PASS.
       - `list_models.py`, `list_models2.py` — fetched model catalogs; wrote `nim_models.json` (102), `ollama_models.json` (19, via `https://ollama.com/v1/models`), `openrouter_models.json` (417), `groq_models.json` (13), `cerebras_models.json` (2), `openai_models.json` (124).
       - `gemini_keycheck.py` — tested all 5 Gemini/Google key variants; only `GOOGLE_API_KEY` returned 200 (50 models), written to `gemini_models_GOOGLE_API_KEY.json`.
       - `schema.sql`:
         ```sql
         CREATE TABLE IF NOT EXISTS llm_eval.probe_run (
             id            bigserial PRIMARY KEY,
             tier          text NOT NULL,
             started_at    timestamptz NOT NULL DEFAULT now(),
             note          text
         );
         CREATE TABLE IF NOT EXISTS llm_eval.probe_result (
             id            bigserial PRIMARY KEY,
             run_id        bigint NOT NULL REFERENCES llm_eval.probe_run(id) ON DELETE CASCADE,
             provider      text NOT NULL,
             model         text NOT NULL,
             probe         text NOT NULL,
             ok            boolean NOT NULL,
             http_status   int,
             latency_s     numeric(6,2),
             detail        jsonb,
             created_at    timestamptz NOT NULL DEFAULT now()
         );
         CREATE INDEX IF NOT EXISTS probe_result_run_idx ON llm_eval.probe_result(run_id);
         CREATE INDEX IF NOT EXISTS probe_result_provider_model_idx ON llm_eval.probe_result(provider, model);
         ```
       - `tier0.py` — full liveness sweep: builds TARGETS list from nim_models/ollama_models/openrouter_free-filtered models plus a BONUS list (google/groq/cerebras/openai/mistral single models); CONCURRENCY = {"nim":2,"ollama_cloud":3,"openrouter_free":5} default 3; PROMPT = "Are you operational? Reply with only the single word YES or NO. No punctuation, no explanation."; retries on 429 up to 2x with 3s*attempt backoff; writes `tier0_results.json`.
       - `tier0_run.log` — full run log (145 targets processed).
       - `tier0_results.json` — raw initial results (65/145 ok, later found 29 of those had empty content).
       - `tier0_patch.py` — reruns the 29 empty-content-but-ok models at max_tokens=400; writes `tier0_patch_results.json` and merges into `tier0_results_final.json`.
       - `tier0_results_final.json` — the authoritative 145-row result set (62/145 truly live), each row has fields: provider, model, base_url, key, ok, status, latency_s, error, content, followed_format, patched.
       - `persist.py` — connects to `postgresql://{DB_USER}:{DB_PASS}@100.91.190.107:5432/casebible` (had to hardcode host/port after DB_HOST resolved to docker-internal "agentos-db" and failed DNS), runs schema.sql, inserts a `probe_run` row (tier='tier0_liveness'), inserts 145 `probe_result` rows. Confirmed run_id=1.
       - `tier1.py` — Tier 1 capability probes:
         ```python
         TOOL_SCHEMA = {"type":"function","function":{"name":"get_case_status","description":"Look up the current status of a legal case by its docket number.","parameters":{"type":"object","properties":{"docket_number":{"type":"string",...},"include_history":{"type":"boolean",...}},"required":["docket_number"]}}}
         TOOL_PROMPT = "What is the current status of case 24-CV-1187? Use the get_case_status tool to find out."
         SUMMARY_SOURCE = "The Riverton County Family Court issued a temporary custody order on March 14, 2026, granting the petitioner, Maria Alvarez, primary physical custody..." (fictional custody case paragraph)
         SUMMARY_KEY_FACTS = ["march 14","alvarez","custody","june 2","parenting class","mediation","april 20","guardian ad litem"]
         INSTR_PROMPT = "Output exactly five words describing a courtroom, separated by single spaces, all lowercase, no punctuation, nothing else before or after."
         ```
         Scoring functions `score_tool_use`, `score_summary` (originally: `words < len(source)*0.5 and hits>=2` — later found flawed), `score_instruction` (regex-based exact-format check). Reads targets from `tier0_results_final.json` filtered to `ok=True`. Concurrency same caps as tier0. Bumped max_tokens: tool_use→500, summarization→500, instruction_following→300 (from originals 200/150/30) to avoid reasoning-model truncation. Writes `tier1_results.json`.
       - `tier1_run.log` — run log (62 targets).
       - `tier1_results.json` — final (rescored/patched in place) results, structure per row: `{provider, model, tool_use:{ok,latency_s,...}, summarization:{ok,latency_s,...}, instruction_following:{ok,latency_s,...}}`.
       - Offline rescoring script (inline, not saved as a named file) relaxed summarization scoring to: `words < 130 and hits>=3 and c.count(".")<=2`, updating `tier1_results.json` in place — raised summarization pass from 15/62 to 44/62 (29 flips).
       - `tier1_patch.py` — imports from `tier1.py` (`score_tool_use, score_summary, score_instruction, TOOL_PROMPT, TOOL_SCHEMA, SUMMARY_PROMPT, INSTR_PROMPT, probe, sem_for`), reruns instruction_following at max_tokens=1500 for 11 specific (provider,model) targets that returned empty content, using base_url/key from `tier0_results_final.json`. Raised instruction_following from 34/62 to 42/62.
       - `persist_tier1.py` — inserts `probe_run` (tier='tier1_capability'), inserts 186 `probe_result` rows (62 models × 3 probes: tool_use, summarization, instruction_following) with `detail` jsonb containing everything except ok/latency_s. Confirmed run_id=2.
       - `tier_embed_rerank.py` — design/script (NOT YET EXECUTED) for embedder + reranker tests. Covers: NIM text embed (NVIDIA_EMBED_TEXT_ID, needs `input_type` param), NIM code embed (NVIDIA_EMBED_CODE_ID), Google embed (`gemini-embedding-001` via native `embedContent`, called once per text since no batch), Mistral embed (`mistral-embed` via OpenAI-compatible `/v1/embeddings`), Cohere embed (gated off, no key — `embed_cohere` returns `(None, "SKIPPED: no COHERE_API_KEY...")`), and NIM reranker (tries two candidate URL shapes: `https://ai.api.nvidia.com/v1/retrieval/nvidia/reranking` and `{NVIDIA_BASE_URL}/ranking`). Test methodology: embed "I adopted a cat last week" / "My dog needs a walk" / "Q3 tax filing is due Friday", pass if cosine(cat,dog) > cosine(cat,tax) and dimension matches where known. Rerank test: 1 query about custody parenting-class deadline + 4 passages (1 relevant, 3 distractors), pass if the relevant passage ranks #1.
   - `C:\Users\matts\.claude\projects\E--AI-Workspace\memory\detection-refinement-ground-truth-labeling.md` — updated with a large "## RESOLVED 2026-08-24" section documenting the labels-never-existed finding across 6 artifacts, plus a note that `analysis.human_label_gold` is a misnomer (not gold — same unlabeled data with extra columns), and that only `ai_flagged` (374 rows, machine detector output) has real content.
   - `C:\Users\matts\.claude\projects\E--AI-Workspace\memory\casebible-llm-classification-run-june-2026.md` — new memory file documenting the June 2026 LLM classification run found in PG16→PG18-migrated `media.enrichment`/`media.screenshots`, including full model/latency/confidence tables, the "two unpublished PG16 containers" discovery, and the "docker exec local socket needs no password" trick.
   - `C:\Users\matts\.claude\projects\E--AI-Workspace\memory\MEMORY.md` — index updated with two new/edited entries pointing to the above two files.

4. Errors and fixes:
   - **psycopg not found on default `python3`**: fixed by locating the uv-managed interpreter at `C:/Users/matts/AppData/Roaming/uv/python/cpython-3.14.6-windows-x86_64-none/python.exe` (already had psycopg 1.5.4-era duckdb etc. installed) and installing psycopg[binary] there via `uv pip install --python <path> --break-system-packages "psycopg[binary]"`.
   - **DuckDB `table_schema` column not found** in `duckdb_tables()` — fixed by using `schema_name` instead (DuckDB's own catalog function naming differs from information_schema).
   - **pg_restore.exe not found locally** (`/c/Program Files/PostgreSQL/18/bin/pg_restore.exe`) — user clarified they meant PG18 was installed on the *VPS*, not locally; pivoted all pg_dump/pg_restore work to run inside the `agentos-db-*` container on ovh-files via `ssh ovh2-ts` + `docker exec`.
   - **Cross-schema custom type dependency**: `reference.*` and `evidence.*` table DDL referenced enum types (`ai.citext`, `ai.mcl_factor`, `ai.category_polarity`, `ai.pattern_match_type`, `ai.sensitivity_tier`, `ai.evidence_tier`, `ai.precision_class`, `ai.review_state`) plus `evidence.acquisition_authority/acquisition_method/record_medium` — none of which existed in the new `casebible` DB. Fixed by extracting each type's exact definition from the source DB with a `pg_get_functiondef`/enum-introspection query and recreating them in `casebible` (types in `public` schema for the ai-schema ones, in `evidence` schema for the evidence-specific ones), then `sed`-rewriting `ai.X` → `public.X` in the dumped SQL before restoring, in three sections (pre-data/data/post-data) to isolate DDL from COPY payloads.
   - **`docker exec $CID psql ... <<'SQL' ... SQL` silently did nothing** — root cause: `docker exec` without `-i` does not forward stdin, so the heredoc was never delivered. Fixed by switching to individual `docker exec $CID psql -U ai -d casebible -c "..."` statements instead of piped heredocs.
   - **Accidental double-run of `cb_ai.sql`** (which contains data COPY statements) during the trigger-function reattachment pass — verified no duplication occurred because the re-run's INSERT/COPY hit primary-key conflicts and rolled back; confirmed via row-count check across 8 key tables, all correct.
   - **`working.event_source_record` ended up empty (0 rows) after the cascade of restores** — diagnosed as data lost to an earlier failed statement in the same section; fixed by re-running just that table's data-section dump/restore, then re-verified count+hash match (445 rows, md5 a7755d7ffb04461b6619147992f27364 on both sides).
   - **Gemini API 400 "API key not valid"** on the native `/v1beta/models` endpoint with Bearer header — fixed by switching to the documented `?key=` query-param auth style; this revealed GEMINI_API_KEY and GEMINI_API_KEY_4 are simply dead/invalid keys (still 400 even with correct auth style), and GEMINI_API_KEY_2/_3 are stale OAuth access tokens (401, "Expected OAuth 2 access token") not real API keys at all — confirmed dead via both query-param and Bearer attempts. Only GOOGLE_API_KEY is live.
   - **Cerebras and (initially) Mistral returned HTTP 402 Payment Required** on their model-list/chat endpoints — reported as dead accounts needing billing; user later said "Mistral is fixed" and it did return OK in the Tier 0 run; Cerebras remained dead (402) through the whole session, unresolved.
   - **Sed mangled the DSN line in `persist.py`** on first attempt (`f"postgresql://{env.DB_USER}:{env.DB_PASS}@{env.100.91.190.107:5432/casebible"` — invalid Python) — fixed with a corrected full-line sed replacement.
   - **DB_HOST from Agno-MCP-Platform.env resolves to `agentos-db`** (Docker-internal hostname, not resolvable from the desktop) — fixed by hardcoding `100.91.190.107:5432` in the persist scripts instead of using `env.DB_HOST`.
   - **Tier 0 false negatives from reasoning models**: 29 of the initial 65 "ok" liveness results actually had empty `content` because the model spent its entire `max_tokens=20` budget on hidden reasoning (confirmed via raw JSON showing a `"reasoning"` field and `finish_reason: "length"` for `ollama_cloud/kimi-k2.6`). Fixed via `tier0_patch.py` rerunning those 29 at `max_tokens=400`; 27 came back with real content, 2 (`poolside/laguna-xs-2.1:free`, `google/diffusiongemma-26b-a4b-it`) stayed genuinely empty/failed, and one previously-OK model (`nvidia/llama-3.3-nemotron-super-49b-v1.5`) errored transiently on retry and was marked False in the final merge. Net final: 62/145 (not the naive 65/145).
   - **Tier 1 summarization scoring was a false-negative bug** (47/62 "failed"): the pass condition `word_count < source_word_count*0.5 AND key_facts_hit>=2` was too strict for a "summarize in exactly one sentence" prompt over a dense fact-heavy paragraph — good, correct summaries naturally ran 60-90 words (not under the ~65-word 50% threshold) while packing in 5-6/8 key facts. Fixed by rescoring offline (no new API calls, reusing already-captured `content` field) with relaxed criteria: `words < 130 (raw source length) and hits>=3 and sentence_like (<=2 periods)`. This raised summarization pass rate from 15/62 to 44/62 (29 flips). Some remaining fails are legitimate/expected: content-safety and topic-control guard models correctly declining to engage, and a translation model (`nvidia/riva-translate-4b-instruct-v1.1`) outputting Arabic since translation is literally its function — these are correctly-scored non-passes, not bugs.
   - **Tier 1 instruction_following had 11 empty/http_error results at max_tokens=300** — same reasoning-model token-starvation pattern as Tier 0. Fixed via `tier1_patch.py` rerunning just these 11 targets at `max_tokens=1500`; 9 of 11 then passed with correct 5-word answers, 2 remained genuine fails (`minimaxai/minimax-m3` produced 4 words not 5, `dots-studio/dots-3-note-preview:free` still empty). Raised instruction_following from 34/62 to 42/62.

5. Problem Solving:
   - **Recovery investigation (fully resolved, no further action needed)**: Definitively established across 6 independent artifacts (Jul 3 CSV, Jul 3 XLSX, Aug 1 15:47 pre-wipe CSV backup, Aug 1 20:41 pg_dumpall.sql.gz, a freshly-booted Aug 3 02:32 recovered PG18 cluster, and the live PG18 `ai` database) plus a contemporaneous log entry (`LOG.md:324`, 2026-07-07) that the ground-truth human-labeling pass for the 1,918-message pilot conversation was **never performed** — not lost, never done. `analysis.human_label_gold` is confirmed to be a misnomer (same unlabeled data, not actual gold-standard labels). This conclusion is now durably recorded in memory files so it is never re-investigated.
   - **Data conflation cleanup (completed for the `ai`-schema slice, then extended)**: Initially moved 24 real rows + dropped 3 empty test tables out of the Agno platform's `ai` schema into a new dedicated `casebible` database. Later, per explicit user instruction, extended this to move ALL Case Bible-shaped data (analysis/reference/working tables from `ai`, plus evidence/working tables from `ai_test_ingest`) into `casebible`, resolving type dependencies as they surfaced. All 24 migrated tables verified byte-identical via count+md5 hash. Source databases (`ai`, `ai_test_ingest`) left fully intact — nothing dropped from them in this final consolidation pass (only the original 4 small tables were dropped earlier, after their own independent verification).
   - **LLM provider capability harness (built and run for Tier 0 + Tier 1, ongoing)**: Built a reusable async test harness spanning 8 providers (NIM, Ollama Cloud, OpenRouter-free, Google, Groq, Cerebras, OpenAI, Mistral; Cohere wired but inactive). Solved two distinct false-negative classes caused by reasoning models consuming token budgets on hidden thought traces before reasoning models solved two distinct false-negative classes caused by reasoning models consuming token budgets on hidden thought traces before answering (both in Tier 0 liveness and Tier 1 instruction-following), and one scoring-threshold bug in Tier 1 summarization. All results persisted to `casebible.llm_eval` (run_id=1 for Tier 0, run_id=2 for Tier 1).
   - **Open/unresolved**: Whether to expand the 5 "bonus" providers (Google, Groq, Cerebras, OpenAI, Mistral) from single reference pings to full catalog sweeps — I asked via AskUserQuestion, got no response after 300s, and defaulted to leaving them as-is per original scope (this default has NOT been explicitly confirmed by the user yet). The embedder/reranker test script (`tier_embed_rerank.py`) is fully written but has never been executed. Whether/when to drop the PG16 source databases (the running unpublished container and the old `casebible-pg` exited volume) is still pending explicit user go-ahead. Cerebras remains dead (402 payment required, needs billing — user has not addressed this).

6. All user messages:
   - "we need to find the test wew submitted to test the differant llms with doc classification that saved in the pg db. maybe ALSO in duckdb/lance local i cant rememeber"
   - "check pg18 on ovh-files also"
   - "So this was actually done a couple months ago you'll find results already saved We can move with this new application front anyways"
   - "Add Gemini free tier to the list An Obama only uses cloud Always nothing local ever Check the secrets folder ~/.secrets" (note: "An Obama" appears to be a speech-to-text mangling; interpreted in context as referring to Ollama, i.e., "Ollama only uses cloud, always, nothing local ever" — a standing rule already reflected in memory as `llm-ocr-provider-routing-strategy` and `nim-embedqa-scoped-not-banned` type constraints)
   - "Why do you need Melvis" (interpreted as asking why Milvus was mentioned — clarified nothing was actually needed from Milvus, it was incidental to a file read)
   - "I do believe PG vector is installed along with the Duck DB extension So just persist everything otherwise everything is on overh trials" (interpreted: pgvector + pg_duckdb confirmed installed, so persist everything; "overh trials" unclear fragment, likely relates to "ovh" infra)
   - "ai.casebible_ingest_test2_contents ~2 ai.casebible_ingest_test_contents ~2 ai.casebible_evidence_test_contents ~1 These don't belong in that in that database that needs to Or the case Bible databases" / "Data conflation here"
   - "Yeah look for the case Bible tables or database"
   - "casebible_evidence_contents 64 kB ~24 casebible_evidence_test_contents 32 kB ~1 casebible_ingest_test2_contents 32 kB ~2 casebible_ingest_test_contents 32 kB ~2 Again I don't think these this database Make a new properly named properly separate database migrate the data that exists that we and let's start fresh Not in AI make a case Bible in that instance"
   - "analysis.human_label_gold (1,918 rows, intact) bec Tell because something all the labels disappeared so it's just a list of messages it doesn eat shit right now"
   - "Any chance of recovery somehow"
   - "Verify the date that the data is there and get rid of them for their solar no confusion" (interpreted: verify dates, then drop the empty/duplicate source tables to avoid confusion)
   - "So I just attached I think the old block storage I THINK that belonged to ovh-data to ovh-app... see if ther3es anything there to recvoer"
   - "E:\AI_Workspace\casebible\exports" / "E:\AI_Workspace\casebible\backups" (pointing me at local directories to check)
   - "ai.casebible_ingest_test2_contents ~2 ai.casebible_ingest_test_contents ~2 ai.casebible_evidence_test_contents ~1 These don't belong in that in that database..." (recurrence — mid-turn interjection, appears to be a duplicate/echo)
   - "Data conflation here" (recurrence)
   - "Yeah look for the case Bible tables or database" (recurrence — see above, associated with `working.chat_chunk_embedding` discovery step)
   - "I didn't mean locally I meant on the VPS but it was installed actually so you might find something"
   - "But I meant more duck DB parquet locally and the VPS version of PG"
   - "Migrate all of that information over if you don't have it also"
   - "So this was actually done a couple months ago you'll find results already saved We can move with this new application front anyways" (recurrence, mid-turn)
   - Final answer message: "yes to the first part however I just realized that the AI test ingest there's probably evidence uh related Now the entire shape changes so I'm just going to update the other project that this is where the test lives now or the previous test information So just Fyi" (in response to my two proposed next steps: finish DB consolidation vs run source census)
   - Rejected two Grep tool calls (reader-check on detection_pattern/behavior_category and on human_label/court_case/matter/context_record) with: "No go ahead and grab the the test tables also"
   - "/model" slash command output (system-generated, not a real user message, but shown as: "Set model to Sonnet 5 and saved as your default for new sessions") followed immediately by: "So now create a set of tests that tests each model's functions as far as tool usage summarization maybe even instruction following if you can figure out how design a set of tests and test all of the models that we have available through NIM So you do a really basic test first just doesn't respond yes or no And if it responds then we can do more tests as well as Olama and for that matter open code but for open code filter free models and then even though it's out of scope for the moment design a test for RE rankers and embedders if possible"
   - "Sorry open router not open code"
   - "Throw Gemini in there for a reference"
   - "Keep in mind Nvidia Nim is a free API so if we hit it too hard we're going to get limited and Olava Cloud I have a pro account which gives me three concurrent models to one"
   - "Multiple models same test figure out what's available"
   - "Could probably also pull menstrual and cohere in any other Apis you can find in there for shits and giggles"
   - "You should grab all of the Gemini and Google keys there should be four of them but for the other three we don't need to run every single model on every single account just run a really cheap model on the other accounts just to make sure that they're live"
   - "Mistral is fixed"
   - "Google can embed"
   - "so can mistral"
   - "and cohear i think"
   - (date-change system reminder, not a user message: "The date has changed. Today's date is now 2026-08-25.")
   - "yes" (in response to my offer to run Tier 1 on the 62 live models)
   - Rejected my `TaskOutput` blocking-wait tool call, and sent: "│ Google │ 1 │ 1 │ live │ │ (gemini-2.5-flash-lite) │ │ │ why only one midel??" (asking why only one Google model was tested in Tier 0)
   - (No response to my AskUserQuestion about expanding bonus providers — timed out after 300s)
   - (Task completion notification for tier1.py, system-generated, not user)
   - The task/tool call to send the compact-summary request itself (system-generated instruction, not a user message).

7. Pending Tasks:
   - Resolve whether to expand the 5 bonus providers (Google, Groq, Cerebras, OpenAI, Mistral) from single-model reference pings to full catalog sweeps — question was asked via AskUserQuestion and got no response; I defaulted to NOT expanding for now, but this has not been explicitly confirmed by the user.
   - Report the final, corrected Tier 1 results (tool_use 50/62, summarization 44/62 after rescoring, instruction_following 42/62 after patching, 34/62 pass all three) back to the user — this had not yet been communicated when the summary request interrupted.
   - Decide whether/when to run `tier_embed_rerank.py` (NIM text/code embed + rerank, Google embed, Mistral embed, Cohere embed-if-keyed) — user asked for this to be "designed" and it is fully built but explicitly called "out of scope for the moment," though the user later added Google/Mistral/Cohere to the embedder design scope, suggesting growing interest in actually running it.
   - Decide whether/when to drop the two PG16 source containers/volumes (`rmj36da884vt5nzueh28mlng` running container holding the old `casebible` DB, and the exited `casebible-pg` container's `casebible_pgdata` volume) now that their data is verified migrated into PG18 `casebible.media`.
   - Decide whether/when to drop the source tables in `ai` and `ai_test_ingest` now that the 24-table consolidation migration has been verified byte-identical (nothing has been dropped from these in the final consolidation pass; only the original 4 small evidence-contents tables were dropped earlier).
   - Address the dangling foreign keys that didn't migrate (pointing to `ops.processing_run`, `working.conversation`, `working.device`, `working.entity`, `analysis.timeline_event`, `evidence.file_node` — all empty in source, so no data violates anything, but referential integrity enforcement is currently absent for these relationships in `casebible`) — I noted "Say the word and I'll create the empty shells to restore them" but no decision has been made.
   - Cerebras account remains dead (402 payment required) — unresolved, would need the user to add billing if they want it included in future runs.
   - Follow the running "otherproject" documentation update the user mentioned doing themselves regarding where the ai_test_ingest SMS evidence corpus now lives (`casebible.evidence.raw_sms` etc.) — this is the user's own action item, not something I need to do, but I should be aware the corpus has a new canonical home if referenced again.

8. Current Work:
   Immediately before this summary request, I had just finished:
   1. Investigating and fixing a summarization-scoring bug in Tier 1 results (rescored 62 models' summaries offline using already-captured content, raising the pass count from 15/62 to 44/62 with relaxed criteria: `words < 130 and hits>=3 and sentence_like`).
   2. Investigating and fixing an instruction-following token-starvation issue for 11 reasoning models via `tier1_patch.py` (max_tokens=1500 rerun), raising instruction_following from 34/62 to 42/62.
   3. Computing final Tier 1 tallies: `n=62 tool_use=50 summarization=44 instruction_following=42 all_three_pass=34`, with a full per-provider breakdown (groq{1,0,1,0}, google{1,1,1,1}, mistral{1,1,1,0}, nim{28,18,17,15}, openai{1,1,1,1}, openrouter_free{11,10,8,7}, ollama_cloud{19,19,15,18}) and a printed list of the 34 models passing all three probes (e.g., google/gemini-2.5-flash-lite, openai/gpt-4.1-nano, several openrouter_free `:free` models, many ollama_cloud models like deepseek-v4-pro/flash variants, kimi-k2.7-code, gpt-oss:20b/120b, nemotron-3-super/ultra/nano, and several nim models like llama-3.3-nemotron-super-49b-v1, gpt-oss-20b, llama-3.1-70b/8b-instruct).
   4. Writing and running `persist_tier1.py`, which created `probe_run` id=2 (tier='tier1_capability') and inserted 186 `probe_result` rows into `casebible.llm_eval`. The final tool output confirmed:
      ```
      Persisted run_id=2, 186 probe results (62 models x 3 probes)
      ('instruction_following', 42, 62)
      ('summarization', 44, 62)
      ('tool_use', 50, 62)
      ```
   This was the last action taken — the summary/compact request interrupted immediately after this persistence confirmation, before I had composed any reply back to the user presenting these final Tier 1 numbers, the scoring-bug corrections I made, or re-raising the still-unanswered question about expanding bonus providers / running the embed-rerank tests.

9. Optional Next Step:
   Report the final, corrected Tier 1 results to the user in a clear summary (per-probe totals: tool_use 50/62, summarization 44/62, instruction_following 42/62, all-three-pass 34/62; per-provider breakdown; note on the two scoring fixes applied — the summarization length-threshold bug and the reasoning-model token-starvation patch — since these materially changed the numbers from the raw first-pass run), confirming the data is now persisted in `casebible.llm_eval` (run_id=2, joining run_id=1 for Tier 0). This directly continues the immediately-preceding work (the last completed action was the `persist_tier1.py` run whose output was `Persisted run_id=2, 186 probe results (62 models x 3 probes)` / `('instruction_following', 42, 62)` / `('summarization', 44, 62)` / `('tool_use', 50, 62)`) and the user's explicit "yes" instruction to run Tier 1 in the first place. I should NOT proceed to run `tier_embed_rerank.py` or expand the bonus providers without further confirmation, since the AskUserQuestion on bonus-provider expansion went unanswered and the embed/rerank tests were explicitly scoped as "out of scope for the moment."
</summary>
