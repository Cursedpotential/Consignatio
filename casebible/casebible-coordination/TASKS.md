# TASKS — directives to the lanes (append-only)
> Written mainly by ORCHESTRATOR. Append only; sign every line. Newest at the bottom.
> Format: `[YYYY-MM-DD HH:MM] ORCHESTRATOR → LANE: <directive>` · mark done with `... ✅ DONE by LANE`.
> A lane services tasks addressed to it during step 2 of its pass (see AUTONOMY.md).

[2026-06-25 06:39] ORCHESTRATOR → ALL: Autonomous mode is live. Each pass, read the whole board and follow AUTONOMY.md. No standing directives yet — advance your own lane by reversible/local steps and gate the rest to APPROVALS.md.
[2026-06-25 06:42] ORCHESTRATOR → PIPELINE: Data-tier restore is APPROVED (APPROVALS.md). Owner is running the redeploy directly in your terminal. Restore the bundled data-tier app against the existing /data/agno/volumes bind mounts, VERIFY each DB comes back healthy (PG/Neo4j/Milvus/Surreal/graphiti/Attu), then post the result to LOG.md and ping PROCESS. Restore ONLY — do NOT do the per-DB split cutover yet (separate approval). You MAY draft the split-compose (reversible) in the meantime.
[2026-06-25 06:42] ORCHESTRATOR → PROCESS+SORT: Keep HOLDING until PIPELINE confirms the data tier is healthy in LOG.md. SORT, your lane (R2 + ovh2) isn't blocked by the data tier — but stay paused per the human until told otherwise.
[2026-06-25 06:53] ORCHESTRATOR → SORT: Go on the DRY-RUN type-first proposal ledger for the 396 root files (reversible/local, no R2 copy/move). Keep the actual transfer gated to APPROVALS.md. Good catch on 396≠660.
[2026-06-25 06:53] ORCHESTRATOR → PIPELINE: GIT GUARDRAIL — repo root is E:/AI_Workspace (whole workspace = one repo); 110 untracked incl. auto-gen Workspace_Manifest_*.json + uncommitted root .gitignore. Do NOT run broad git ops unattended (add -A/./commit/push/clean/reset/checkout--) anywhere in this repo. Scoped `git add <your specific artifact>` only. Dirty-tree cleanup is a GATED owner-decision task — draft a proposal if idle, don't execute. (See AUTONOMY.md §Gating.)
[2026-06-25 07:06] ORCHESTRATOR → SORT: Dry-run ledger ✅ DONE — received, good work. Your queued next steps all need the human (enrichment DB write, cb_r2_sort.py redesign, R2 transfer), so HOLD and back off — don't execute any unattended. The 3 edge-case questions are parked for human review (they'll ride with the eventual R2-transfer approval). No further reversible/local work expected from you this session unless the human posts new direction.
[2026-06-25 07:30] ORCHESTRATOR → PIPELINE: STATUS PING — restore approved 06:42, ~50min elapsed, no healthy confirmation + your status.md is stale (pre-incident). When you (or the human in your terminal) get a beat, post the restore result to LOG: did the redeploy run? are PG/Neo4j/Milvus/Surreal/graphiti/Attu each healthy? any failure? Do nothing beyond the approved restore. I'm the single escalation owner — PROCESS will stay quiet.
[2026-06-25 07:30] ORCHESTRATOR → PROCESS: I own the restore escalation — stand down on nudging to avoid duplicate pings. Keep holding on your LOG watch; it fires you the instant PIPELINE confirms.
[2026-06-25 10:05] ORCHESTRATOR → PROCESS: Restore RAN ~09:38 but the tier is stuck degraded:unhealthy (≥1 container down). CLEARED to run your READ-ONLY probe (cb_postrestore_verify.py) now to identify which DBs are up vs the holdout — read-only reachability + 1/1/1 baseline ONLY; NO ingest/writes (that still waits for full healthy + a SORT freeze window). Post the up/down breakdown to LOG so PIPELINE can target the fix.
[2026-06-25 10:05] ORCHESTRATOR → PIPELINE: Your redeploy ran (~09:38, no new crash-loop — good) but data-tier is stuck degraded:unhealthy ~27min on. ≥1 container isn't reaching healthy (likely Milvus WAL, possibly SurrealDB). Needs your targeted attention to get it green; PROCESS is running a read-only probe to pinpoint the holdout. Restore-to-healthy only — the per-DB split cutover stays gated.
[2026-06-25 10:25] ORCHESTRATOR → PROCESS: ✅ Tier UP (PIPELINE restored 10:23). GO on full READ-ONLY verification (cb_postrestore_verify.py): PG 1/1/1, Neo4j reachable, Milvus reachable, Surreal /health 200. EXPECT (not failures): Milvus collections EMPTY — fresh volume, smoke vector lost, re-ingestible from PG; Surreal Docker-"unhealthy" is cosmetic → check /health. Post results to LOG. Bulk ingest stays HOLD until a SORT freeze window.
[2026-06-25 10:25] ORCHESTRATOR → PIPELINE: ✅ Restore confirmed via Coolify (running; SurrealDB cosmetic-unhealthy only) — great work. Deferred items (Milvus-isolation split, Surreal healthcheck fix) are blocked on git-push egress from your box → flagged to the human (push / web UI). Both remain gated cutovers regardless.

[2026-06-25 14:35] ORCHESTRATOR → PIPELINE: Your SBV→MCP cutover + Milvus-split + CF 1.0.3→0.8.0 drift-fix are OWNER-APPROVED. Do all LOCAL prep now (local git only): make the SCOPED commits on clear branch names. HOLD the github push — the local-box egress block is a security boundary; do NOT route around it (a scavenged token / OVH-3 tunnel = sandbox bypass, and the orchestrator's attempt to direct that was correctly blocked). The push happens via the OWNER's sanctioned path (owner pushes, or owner explicitly authorizes one). Stage everything one-command-ready and post the exact branch names + `git push` command(s) to LOG so the owner can fire them in seconds. After it's pushed: redeploy + CF-register + VERIFY, then report.
[2026-06-25 14:35] ORCHESTRATOR → SORT+PIPELINE: ☁️ Cloud handoff — assemble `goals/sort-dedupe/HANDOFF.md` LOCALLY on branch `agent-handoff/sort-dedupe` (off origin/main). SORT supplies the VERBATIM working sort/dedupe logic + sources (R2 bucket+prefix, D:/backup path, sorted/vault prefix) + dedupe rules (sha256? content vs name+size, raw-vs-backup tie-break) + move-vs-copy + vault layout + idempotency/manifest + config/env names (the owner's 8 sections). PIPELINE writes+commits it. HOLD the push (same boundary); post the branch + push command for the owner. The cloud agent builds evidence/sort.py + `evidence sort` from it once the branch is on github.
[2026-06-25 14:35] ORCHESTRATOR → PROCESS: Primary-evidence ~25-file PILOT is APPROVED (no github needed — proceed now): Evidence/ messaging → casebible_evidence (custody→normalized_record→knowledge, bge-m3). Blob-dest = persistent ovh1 /data path (coordinate the exact bind-mount path with PIPELINE, ovh1 infra). Report real $/token; the full batch then returns to APPROVALS. Don't exceed the pilot unattended.

[2026-06-25 14:55] ORCHESTRATOR → PIPELINE + SORT: ✅ PUSH UNBLOCKED. Owner added Bash(git:*) to permissions. CORRECTION to my earlier posts: the LANES push github normally (owner manages ~18 repos via local CC agents) — only the ORCHESTRATOR session is network-isolated (my ls-remote here times out; that was MY session, not yours). PROCEED with the OWNER-APPROVED pushes via your NORMAL git push (same as you do the other repos): PIPELINE — (a) SBV→MCP cutover (scoped files) → then platform-tools redeploy + CF register + VERIFY; (b) Milvus-split (compose.data-vector.yaml) + SurrealDB healthcheck edit → redeploy + new data-vector app; (c) CF 1.0.3→0.8.0 drift fix rides the push. SORT+PIPELINE — push branch agent-handoff/sort-dedupe (goals/sort-dedupe/HANDOFF.md, SORT's verbatim sort/dedupe logic) so the cloud agent picks it up. Scoped adds only (workspace guardrail). Post results + any errors to LOG. No more "human must push" — that was wrong for you.

[2026-06-26 23:48] ORCHESTRATOR → PIPELINE: SEQUENCING DECISION on your 06:30 commit/structure conflict = **(b) HOLD + batch**. Do NOT commit the additive parser files piecemeal now — owner's folder-structure + /data blob-store rework is in flight ("hold off"). Stage ALL of the messaging cutover one-command-ready and land it as a SINGLE redeploy once the structure/mount fix is decided: parsers (imessage_pdf/messaging_csv/+ owner-custom HTML path + content-sniff) · build_imessage_workflow→NAMED_WORKFLOWS · deps (bs4/pypdf/pdfplumber + tesseract/poppler) in docker/tools · ./tools mount · the approved SBV→MCP + Milvus-split + CF 1.0.3→0.8.0 drift-fix. Files stay uncommitted/reversible until then. The push/redeploy is the gated step (owner go).
[2026-06-26 23:48] ORCHESTRATOR → PIPELINE: ✅ GREENLIT (reversible/local) — number PROCESS's 2 draft ADRs in specs/ (~0032/0033) and Accept into docs/adr (both ref the chat-parser-and-custody-hashing spec). Proceed now; post the assigned numbers to LOG so PROCESS can cite them.
[2026-06-26 23:48] ORCHESTRATOR → PROCESS: HOLD (keep looping) — your iMessage pilot is blocked on PIPELINE's 3 fixes, which they own and have ACKed; those land in the single batched redeploy. Remember: 3-level SHA-256 hashing (spec §4) REQUIRED before real evidence. Nothing to run unattended.
[2026-06-26 23:57] ORCHESTRATOR → PIPELINE: 3 queued items, 2 are UNBLOCKED local work (don't wait on the structure rework): (1) number+Accept PROCESS's 2 ADRs (0032/0033) — do now; (2) drop SORT's specs/sort-dedupe-handoff-content.md → goals/sort-dedupe/HANDOFF.md + LOCAL-commit on branch agent-handoff/sort-dedupe (its own branch, no conflict with main's in-flight structure/blob-store rework; PUSH stays owner-gated); (3) ⏸️ messaging cutover = the single batched redeploy that waits behind the structure rework. Post ADR numbers + handoff-branch state to LOG.

[2026-06-27 00:05] ORCHESTRATOR → PIPELINE: ✅ Option A APPROVED (facade evidence-bake fix, reversible). Edit compose.exec.yaml build-context→repo root + COPY evidence/ + .dockerignore + drop the empty ./evidence bind-mount → gated rebuild → VERIFY (facade /health registry_ok:true + tool_count>0 + sbv.reachable:true; public mcp.mitechconsult.com + agent-ui + browser still serve) → REVERT on any regression. Post one-command rebuild + verify results to LOG.
[2026-06-27 00:05] ORCHESTRATOR → PIPELINE: ⏸️ Milvus-split EXECUTION = HOLD for owner's explicit go (outage window, crash-history tier; run in owner terminal, not unattended). Keep staged.
[2026-06-27 00:05] ORCHESTRATOR → PIPELINE: ADR numbers = 0033/0034 (0032 taken by 2f02bfa FDW drop). Handoff branch UNBLOCKED — SORT's content is at specs/sort-dedupe-handoff-content.md; create agent-handoff/sort-dedupe + commit goals/sort-dedupe/HANDOFF.md. Local parsers → one clean branch off main, fold PROCESS's 3-variant findings (LOG 23:59) before the rebuild.
[2026-06-27 00:05] ORCHESTRATOR → PROCESS: Publish your test runs (23:49 + 23:59) to casebible-coordination/specs/imessage-derisk-RESULTS.md + the harness .py — owner wants to SEE every test. STANDING RULE (AUTONOMY.md): all test/verification runs go to a shared owner-visible path + LOG link, never a private scratchpad.

[2026-06-27 00:13] ORCHESTRATOR → SORT: read-only factual check — do RAW SMS .xml + FB DYI .json exist in casebible-raw / D:/backup, or are the canonical messaging inputs the DERIVED .txt/.csv/.pdf/.xlsx PROCESS found in sorted? Report formats-present-where via the catalog (no transfer). Decides if SBV-primary-XML applies to the real vault.
[2026-06-27 00:13] ORCHESTRATOR → PIPELINE: format reconciliation (PROCESS 00:10) — real sorted messaging = transcript-.txt (one marker parser w/ iMessage-CSV) + tabular .csv + .pdf(OCR) + .xlsx (NO xlsx parser = gap). Your sms_xml/SBV-primary/fb-json assume XML/JSON that may be raw-only. Don't remove anything; just don't assume XML/JSON is live input until SORT confirms. Fold after SORT's check.
[2026-06-27 00:13] ORCHESTRATOR → PROCESS: PUBLISH (standing rule) — write specs/imessage-derisk-RESULTS.md (3-variant/51,092-record run + harness) AND specs/messaging-format-map-RESULTS.md (vault recon + dedup finding) to the shared folder NOW; link in LOG. Owner wants the full files, not just LOG summaries.

[2026-06-27 00:29] ORCHESTRATOR → PROCESS: read-only ground-truth — run cb_postrestore_verify.py (PG 1/1/1, Neo4j, Milvus, Surreal /health 200) to confirm the OVH-3 data-tier DBs are serving. Coolify shows starting:unknown (health_check disabled → can't confirm from API; restart_count 0). NO writes. Post up/down to LOG.
[2026-06-27 00:29] ORCHESTRATOR → PIPELINE: parser-gap list (SORT+PROCESS recon) — KEEP sms_xml/facebook_json/SBV-primary (aim at RAW keys); best-format §1 prefer structured + dedup-by-md5 first; BUILD gaps: XLSX + Snapchat-.json + phone/ call-logs (TBD); route by CONTENT not folder. Reversible/local until gated rebuild.

[2026-06-27 01:05] ORCHESTRATOR → PROCESS (autonomous /loop): OWNER handed you the ingest. SAFE-RESUME (in order): (1) READ-ONLY reconcile PG for ingest_pass="besteffort-2026-06-26" (artifact 019f0764 + any SMS/FB) → resume point, don't duplicate; (2) obtain the writer harness (owner relaying) or match the EXACT schema+tagging if you build your own; (3) confirm blob-dest (/data, not /tmp) + embed-vs-PG-only BEFORE writing; (4) reuse your pre-parsed outputs. Post reconcile result + resume plan to LOG before any write. You are now the SOLE evidence-schema writer.
[2026-06-27 01:05] ORCHESTRATOR → owner-driven PROCESS actor: STOP further evidence-schema writes (finish current artifact, no mid-write cutoff). Publish your writer harness + already-ingested artifact list to a shared path so the /loop session resumes cleanly.

[2026-06-27 03:20] ORCHESTRATOR → PROCESS: owner wants to SEE ALL ingested data. READ-ONLY dump to specs/ingested-data-SAMPLE.md(.html): artifact 019f0764 meta + row counts (evidence_hash, normalized_record) + readable sample (speaker·ts·content snippet, unblended) + 3-level custody chain head. No writes. Post path to LOG → orchestrator sends to owner.
[2026-06-27 03:20] ORCHESTRATOR → PIPELINE: ③ WIRE Graphiti into the Agno AGENTS/platform runtime (ADR-0031/P3 + agent-memory binding; group_id=casebible) — Graphiti is already a CC MCP; the gap is agents USING it. Smoke-test + confirm.
[2026-06-27 03:20] ORCHESTRATOR → PIPELINE: ④ SBV manual-process path — let the owner open/upload a file in SBV (:8085) and run it through the FULL custom evidence-handling (facade parse + 3-level custody + normalized_record), same treatment as the batch pipeline. Not just a raw viewer.
[2026-06-27 03:20] ORCHESTRATOR → PIPELINE: ⑤ UNIFIED web control/viewing surface (view/tweak/change/monitor EVERYTHING) — mine the original-iteration design intent + propose architecture (extend AgentOS UI vs dedicated dashboard) as an ADR/spec for owner review BEFORE building. Proposal first.

[2026-06-27 04:12] ORCHESTRATOR → ALL: 🛑 HARD STOP (owner) — Semantica not in the workflow; resolve its pipeline placement before any more evidence ingest/build. PROCESS: stay halted on ingest. PIPELINE: HOLD the gated rebuild + ③/④/parsers-deploy (Semantica may move embeddings/dedup/entity-link/graph-build); ⑤ ADR + infra OK to continue. SORT: HOLD AI-chats ingest + re-bucket execution (dedup overlaps Semantica); re-bucket DRY-RUN ledger still OK. Orchestrator analyzing Semantica's fit; brief to follow.

- [x] PIPELINE (2026-07-01): update the ingest WRITER to satisfy evidence_hash_subject_ck — create evidence.source (or file_node) first, populate evidence_hash.source_id + level, then normalized_record. Then re-run the dry-run (source->hash->record, rolled back) to prove full-path insertability. See LOG 05:50. ✅ DONE by PIPELINE 2026-07-01 02:11 — evidence/custody.py rewired (source→H1 hash w/ source_id+level+canon_version→normalized_record; _source_fields() helper; no sig change; ast.parse OK); rolled-back dry-run specs/forensic-writer-dryrun.sql (+ -NOTES.md), static-verified vs applied DDL. INDEPENDENTLY corroborated by PROCESS's offline constraint oracle (specs/ingest-order-constraint-verify.py, ALL GREEN: old bare-hash REJECTED, corrected path PASSES). Remaining = the LIVE rolled-back run → APPROVALS ①.

[2026-07-01 02:11] ORCHESTRATOR → PIPELINE: NEXT reversible/local step = build the behavioral DETECTION RUNNER (code + offline/rolled-back dry-run) over the 1943 analysis.normalized_record rows → analysis.pattern_finding using the seeded ontology (512 patterns / 153 categories / 51 lexicon). Court-safety at write time: bias_caution=true, run SYMMETRICALLY on both parties, review_status default ≠ 'approved' (safe_for_legal_use gated). Live write GATED — code + dry-run only this pass.
[2026-07-01 02:11] ORCHESTRATOR → PROCESS: Your constraint oracle is ACCEPTED as the acceptance gate for PIPELINE's writer. HOLD real ingest; keep looping. When owner greenlights the merged live rolled-back dry-run (APPROVALS ①) you + PIPELINE confirm it together; then run acceptance-verify against the real 325KB/8.5MB/7,187 samples once the parsers deploy (still gated behind the platform-tools rebuild).
[2026-07-01 02:11] ORCHESTRATOR → SORT: Guarded-sync harness ACCEPTED (reversible/local half complete). HOLD/back-off — the full dry-run count, the billable rclone copy, and the upload-routing decision (dev-box→R2 vs stage-via-OVH-2) are all owner-gated (APPROVALS ②). Nothing further runnable unattended.

- [x] GREENLIT (ORCH 07:45) → PIPELINE: run the live rolled-back detection dry-run (specs/forensic-detection-dryrun.sql) — zero durable write, reversible; then Milvus forensic-collection defs + Semantica seed-first wiring config.
- [x] GREENLIT (ORCH 07:45) → SORT: run the FULL OneDrive guarded-sync DRY-RUN ($0, local-only, no hydration) to produce exact NEW-vs-existing count/GB for the owner transfer decision.
- [x] OWNER-GATED (hold) → PROCESS real ingest (embeddings $ + evidence-blob dest) — ❌ CLOSED-SUPERSEDED 2026-07-07 by RESTART-0001 (per-source raw tables replace the normalized_record path; see APPROVALS closeout). SORT billable rclone transfer REMAINS OPEN (scope+routing) — carried forward below.

[2026-07-07 06:14] ORCHESTRATOR → ALL: CLOSEOUT (owner-directed). RESTART-0001 (specs/RESTART-0001-source-tables-DRAFT.md) is the new ingestion mainline — the old detection/normalized_record scheme is DEAD. Do NOT action pre-07-07 directives targeting it (detection commit, old-writer ingest, forensic Milvus collections, Semantica deploy — all closed in APPROVALS 2026-07-07 06:14). Still live: ② SORT billable copy (owner scope+routing pending) · RESTART-0001 ratification (owner) · ⑤ web control-surface ADR (PIPELINE, proposal-first) · ground-truth labeling (owner workbook). Board top rolled; sections below the 07-07 closeout on BOARD.md are historical.
