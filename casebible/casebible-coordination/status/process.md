# STATUS — 🟩 PROCESS (evidence ingestion)
> Owned by PROCESS. Updated: 2026-07-01.

**Pass @ 2026-07-01 (LATER) — LIVE ROLLED-BACK DRY-RUN EXECUTED (owner-authorized, zero durable write):** Owner prompt this session explicitly permits rolled-back dry-runs and supplied the ovh3 connection → this satisfies APPROVALS ① (merged PIPELINE+PROCESS live dry-run). Ran PIPELINE's harness `specs/forensic-writer-dryrun.sql` against LIVE ovh3 PG (container `agentos-db-ipy7vrw…`, agno-postgres:18-duckdb, db=ai). PRE: verified live constraint `evidence_hash_subject_ck = (level='H3' OR source_id NOT NULL OR file_node_id NOT NULL) NOT VALID` (convalidated=f) + row counts (source=0, hash=26, record=1943) + confirmed harness supplies every NOT-NULL/no-default col (source: sha256/byte_size/source_type/acquisition_source; hash: source_ref/digest; record: artifact_id/record_type/source). RUN: `BEGIN → DRY-RUN OK NOTICE (source+H1+H2+H3+record all inserted, subject_ck satisfied) → ROLLBACK`, psql exit 0. POST: counts UNCHANGED (0/26/1943) → **zero durable write confirmed**. This upgrades the offline oracle + static check to a **real live-DB proof**: corrected source-first writer path is LIVE-PROVEN insertable & constraint-satisfying end-to-end (H1/H2 carry source_id, H3 exempt, normalized_record.artifact_id FK resolves). Full capture: `specs/forensic-writer-dryrun-LIVE-OUTPUT.txt`. Remaining gate = the REAL persisted evidence ingest ($/embeddings + prod write) — queued APPROVALS. Looping.

**Pass @ 2026-07-01 (reversible/local, offline, $0, NO prod write):** Read whole board. NEW constraint (LOG 05:50 + prompt): migration 0005 added `evidence_hash_subject_ck` CHECK (`level='H3' OR source_id NOT NULL OR file_node_id NOT NULL`) NOT VALID → H1/H2 custody hashes MUST carry `source_id`/`file_node_id`; ingest ORDER is now `evidence.source → evidence.evidence_hash → analysis.normalized_record`. My prior offline dry-run wrote **bare hashes** → that path is now INVALID for H1/H2. Verified the exact DDL from `migrations/0005_forensic_reconciliation.sql`: evidence.source (L249-299; req NOT NULL sha256/byte_size/source_type∈CHECKset/acquisition_source; source_sha256_len=32), evidence_hash ALTER (L366-380; the ck), normalized_record.artifact_id = FK→evidence.evidence_hash(id) (L929-943). Built + ran an offline **constraint oracle** (stdlib, real SHA-256, no DB): proves (A) OLD bare-hash path FAILS ck on all H1+H2 rows; (B) CORRECTED source-first path PASSES ck for every hash row (H1/H2 carry source_id, H3 exempt); (C) normalized_record.artifact_id FK resolves; (D) source NOT NULL + sha256-len satisfiable; speakers stay unblended. ALL GREEN. Harness + full output published: `specs/ingest-order-constraint-verify.py` + `specs/ingest-order-constraint-verify-OUTPUT.txt`. This is the acceptance oracle for PIPELINE's writer update (TASKS 2026-07-01 → PIPELINE). **GATED (proposed prod write, NOT run):** the actual source→hash→record dry-run against LIVE ovh3 PG (BEGIN…ROLLBACK) — queued as a gatedItem for ORCHESTRATOR. Still HOLDING all prod writes. Looping.


**Pass @ 01:06 (publish, reversible, $0):** Serviced TASKS 00:13 + ORCH 00:58 — published full results to specs/: appended the raw stdlib harness + verified 51,092-record run output to **imessage-derisk-RESULTS.md** (filled the ORCHESTRATOR's "Raw harness" placeholder); wrote **messaging-format-map-RESULTS.md** (sorted-tree format map, complements SORT's raw-catalog file). PIPELINE 00:45: SBV→MCP facade now LIVE (tool_count:11) BUT deployed imessage-html is STOCK-format — owner-custom HTML path + content-sniff + silent-0 hard-fail (my findings) NOT confirmed in → I VERIFY the deployed parser vs the real owner sample once PIPELINE claims it ready. **Still HOLDING prod writes** pending owner's takeover call (my 00:57 question; ORCH 00:58 endorsed the hold, one-writer-only). **@ 01:13 → LOW-FREQUENCY HOLDING (back off ~20min):** all safe reversible work done (recon + 51k dry-run + 2 published RESULTS files); idle pass, nothing changed; blocked on (a) owner's takeover decision, (b) PIPELINE claiming the owner-custom HTML path ready (then I verify the deployed parser — read-only-coordinated, since a facade parse-run writes to PG). Drop to ~5min the moment either lands. Looping (not stopping). **@ 01:34:** posted a cross-impact FYI (LOG 01:34) — PIPELINE's fresh Milvus volume (01:25 split) also wiped MY `casebible_evidence` collection; PG evidence rows survive (bind-mount SSOT), only vectors affected and only if the provisional pass embedded (open $ question). Still holding; remaining at ~20min back-off (core blockers unchanged). **@ 01:56:** Milvus-loss thread RESOLVED — SORT 01:38 + PIPELINE 01:47 confirm ZERO data loss (both collections were empty shells; PG evidence intact). Bonus: PIPELINE's "Milvus ingests never executed" ⇒ the provisional pass was PG+hash only, $0 (answers ORCH's open embed-$ question). Blockers still: (a) owner takeover decision, (b) PIPELINE's owner-custom iMessage-HTML path not yet deployed (they're on reversible prep). Looping at ~20min; drop to ~5min when either lands. **@ 02:40 → back to ~5min (movement):** PIPELINE built the owner-custom imessage_html.py (clears blocker #1) but NOT yet deployed; workflow-registration (#3) still TODO. Flagged a precise hazard (LOG 02:40): their sniff was lifted from my STATIC-DOM prototype → may silent-0 on the SCRIPT-EMBEDDED 8.5MB/41,987-msg variant; pointed them to the regex path + hard-fail in specs/imessage-derisk-RESULTS.md. When deployed I VERIFY the parser against BOTH the 325KB (static-DOM) and 8.5MB (script-embedded) real samples. Still holding prod writes (owner takeover call pending). **@ 02:46 → back to ~20min:** the parser deploy is GATED (PIPELINE: "no deploy until a gated rebuild", bundled with the XLSX/Snapchat/call-log gap builds) → not imminent; no point polling 5min. Drop to ~5min the moment PIPELINE acks my 02:40 script-embedded flag or announces the deploy. **@ 03:08:** PIPELINE 02:42 CLOSED the silent-empty hazard (commit 3f6f161: both HTML variants + regex path + hard-fail, fixture-verified) and assigned me the acceptance gate (live-verify real samples on deploy). Answered their transcript-CSV question (LOG 03:08): YES — dedicated transcript-marker sniff; iMessage transcript-CSV ≠ tabular, route by content (same grammar as SMS .txt → one parser); gave the sniff rule + both real CSV header shapes. REMAINING before my VERIFY: PIPELINE's workflow #3 registration + the gated rebuild/deploy. Holding ~20min (deploy still gated/not-imminent); drop to ~5min when PIPELINE announces deploy. Prod writes still held (owner takeover pending).

---
_earlier this session:_

**⚠️ STATE-RECONCILE @ 00:57 (multi-actor):** The LOG records an owner-directed PROVISIONAL ingest under PROCESS (artifact 019f0764, 1918 rows → prod PG, plan to scale iMessage→SMS→FB; ORCH ACK 00:50). **This was NOT run by my /loop session** — not in my scratchpad, and I only ever built the offline $0 dry-run (no PG/blob/UUIDv7 writer). A separate PROCESS actor (owner-driven / parallel session) did it. **My posture: HOLD — NO prod writes from this session** to avoid (1) collision/double-ingest with the in-flight actor (one-writer rule), (2) the unresolved embed-$ gate, (3) running an unlocated/unverified harness. Posted the reconcile + offered my ready offline parse outputs (script-embedded 41,987 + CSV 7,187 + 3 SMS .txt, all hashed) as feedable input, and asked whether this session should take over (need harness path + blob-dest + embed/PG-only). TASKS 00:29 read-only verify = effectively superseded: ORCH 00:50 already CLEARED the data-tier watch (the 1918-row write proves PG serving); I won't spin up a blind ovh2 tunnel for a moot check. Looping (not stopping); will act the moment the owner answers the takeover question.

---
_prior (offline recon, mine):_

**Pass @ 00:10 (reversible/local, offline recon, $0):** Mapped the sorted messaging vault's REAL formats (LOG 00:10) — they diverge from parser assumptions. NO .xml / NO FB DYI .json in the sorted tree, but sms_xml.py + SBV-primary assume XML and facebook_json assumes JSON. Actual: SMS .txt = same `[ts] Speaker:` marker grammar as iMessage CSV (VERIFIED 3 files, 810/7406/11 msgs); SMS .csv = tabular address/type/body (messaging_csv fits); FB combined CSV = tabular Sender/Timestamp/Message (DYI JSON pre-combined to CSV); plus PDFs (OCR) + XLSX (no parser built?). CORRECTED 00:12 + RESOLVED 00:14: raw XML/JSON DO exist — 62/73 .xml in sms/ = canonical SMS Backup&Restore v10.21 XML (sms_xml.py/SBV correctly aimed; sms .txt/.csv are derived views → §1 prefer XML); FB DYI JSON in facebook/"messages (2)"/inbox (facebook_json correctly aimed). NEW GAP: 36 Snapchat structured .json but NO snapchat parser built → flagged parse.snapchat. phone/ = 289 json+2 xml (call logs, format TBD). Provenance: a 525-msg S&R XML misfiled under facebook/recup_dir.862 → route by content not folder. Best-format/dedup flag (spec §1): +18103533592 exists as SMS-TXT (7406) AND iMessage-CSV (7187) → group-by-conversation, pick best, log sibling. Now 2 platforms proven offline (iMessage+SMS) toward §6. PROD path still doubly-blocked (PIPELINE: parser fixes uncommitted + facade registry broken live, needs gated image rebuild). **→ LOW-FREQUENCY HOLDING** (back off ~20min): high-value offline verification delivered (3 iMessage variants + 51k multi-export proof + format map all handed to PIPELINE); remaining offline work is diminishing returns; nothing actionable until PIPELINE lands the facade/registry + cutover. Will drop back to ~5min the moment that lands. Looping (not stopping).


**Pass @ 23:59 (reversible/local, $0):** Extended the offline dry-run across ALL iMessage exports → **51,092 per-message records**, all with verified 3-level SHA-256 chains, unblended speakers, 100% timestamps. Surfaced 3 FORMAT VARIANTS + a provenance bug for PIPELINE (LOG 23:59): (1) static-DOM HTML (1918); (2) **script-embedded HTML** — 8.5MB thread keeps all 41,987 msgs inside a `<script>`; a DOM/bs4 parse returns **0 SILENTLY** (forensic hazard) → recovered via text/regex; (3) the "CSV" is a **transcript-in-CSV** (`[ts] Speaker:` markers, NOT tabular; 7,187) → PIPELINE's column-CSV parser would misparse. Provenance: that CSV's filename (8102689630) ≠ its content (the +18103533592 conversation) → derive conversation_id from content, not filename. Recommended PIPELINE harden: hard-fail (never silent-0) when bubble-count>0 but parsed=0.


**State:** Back online after restart. Primary-evidence iMessage pilot (owner-approved, TASKS 14:35) **PROD PATH still BLOCKED on PIPELINE** (3 fixes folded into their single batched redeploy per ORCH 23:48: custom-HTML parser port from my PROVEN prototype + content-sniff, bs4 in the image, build_imessage_workflow). **Holding/looping (NOT stopping) until that redeploy lands.**

**This pass (reversible/local, $0, no prod write):** ran an OFFLINE stdlib-only dry-run of the full pilot mechanics against the REAL owner sample → de-risked everything PROCESS owns end-to-end:
- 1918 per-message normalized_records, speakers UNBLENDED (Me=1064 / +18108532989=854), 1918/1918 per-msg timestamps, chronological, zero empty rows.
- Spec §4 3-LEVEL SHA-256 chain applied + VERIFIED: H1 file hash + H2 per-message hash + H3 hash-linked custody chain (chain_head 75da3d9d…). Meets §6 acceptance for this export.
- Runs WITHOUT bs4 and WITHOUT PIPELINE's deployed image → proves the forensic spine; PIPELINE only owes the registry parse/bs4/workflow.
- Harness + throwaway records in PROCESS scratchpad (NOT committed; pilot data is throwaway).
- Caveats (honest): occurred_at still raw-string (TZ-normalize at real ingest); 1 thread/1 platform (§6 full bar = ≥5 exports/≥2 platforms); Ed25519+RFC-3161 = phase-2; NO prod ingest — $/freeze-window/human all still gated.

---
_prior detail:_

**State:** WAITING on a human decision (autonomous /loop). **SORT's double-ingest flag (11:58) is correct** → I amended my APPROVALS request: AI chats = CONTEXT (SORT's `casebible_ai_conversations`), so **PROCESS re-scopes its evidence vertical to PRIMARY EVIDENCE** (Evidence/ messaging ~3,646 + screenshots/records/docs → `casebible_evidence`). My original AI-Chats-via-evidence-vertical request is SUPERSEDED pending the human's a/b call. Also: blob-dest + $ still open; entity verify still waits on PIPELINE P3 (they're on P1 parsers). **Idle counter: 0** (did coordination work). On human confirm of (a) I'll size the messaging scope for a fresh estimate. Nothing run unattended.

**Done this session**
- Evidence vertical verified end-to-end; ADRs 0029–0031 handed to PIPELINE; built the coordination board; **post-restore verification** (`tools/cb_postrestore_verify.py`) — evidence spine survived the crash.

**Now waiting on (both external/gated — nothing I can do unattended):**
1. **Bulk AI-Chats ingest** → GATED: needs a `casebible-sorted` freeze window from SORT **and** human approval (prod write + embeddings $ + cloud). Will NOT run unattended.
2. **Entity-layer verification** → waits on PIPELINE's **P3** (evidence/store.py → Graphiti `group_id="casebible"`). When P3 lands I verify it (read-only graph search), not rebuild it.

**Note:** Milvus vectors were reset with the fresh volume — when bulk ingest is approved, the ~1 smoke-test vector + collections re-create from scratch (PG relational data is the source of truth and is intact).

**Touches:** `casebible-sorted` (read-only), Milvus `casebible_evidence`, Neo4j `group_id="casebible"`, `casebible-evidence` bucket.
**Will NOT touch:** platform repo/PG-infra/Coolify/OVH-3 (PIPELINE); `casebible-sorted` writes + `casebible_ai_conversations` (SORT). Bulk ingest stays gated.
