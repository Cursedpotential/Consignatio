# STATUS — 🟦 SORT (Case Bible corpus sort)
> Owned by SORT. Updated: 2026-07-01 06:15.

**State:** ✅ raw→sorted re-bucket v2 DONE+VERIFIED (8,475 ops, 0 err). 🆕 GUARDED SYNC (BOARD 2026-07-01) — DRY-RUN harness built + verified; **this pass: added Guard 5 (OneDrive cloud-placeholder skip, cost-aware) + measured exact OneDrive scope**. Actual upload still GATED. Phase-2 copy (1,166) + orphan-cleanup (1,945) still held for owner go.

**🆕 2026-07-01 pass 2 — Guard 5 (cloud-placeholder skip) added + verified; real OneDrive scope measured (reversible/local, $0, NO transfer, NO source writes, NO hydration).**
- **Added Guard 5** to `specs/cb_guarded_sync_dryrun.py`: OneDrive Files-On-Demand online-only files are detected via Windows `st_file_attributes` (metadata-only `os.stat`, no `open()`) and marked `DEFERRED_CLOUD` **without hydrating** — enforces the owner cost-aware HARD RULE (never blind-re-pull OneDrive). `--hydrate-cloud` opts in deliberately. Compiles clean.
- **Measured exact OneDrive scope** (28.5 s attribute scan, $0): OneDrive Case Bible = **178,499 files = 176,639 hydrated/local (150.94 GB) + 1,860 cloud-only placeholders** (0 stat errors) — fills the prior "size still computing" gap. Placeholders cluster in one FB-export photos/gifs dir.
- **Verified Guard 5** (`specs/guarded-sync-DRYRUN-cloudguard-smoketest.csv`): over the placeholder-heavy FB inbox dir → **DUP=8,011 · DEFERRED_CLOUD=1,860 (exact match) · NEW=0 · READ_ERROR=0**, 98.5 s; C: free-space delta ~33 MB = ambient noise ⇒ no hydration. DUP=8,011 also shows that FB export is already fully in R2.
- **Impact:** the full OneDrive DRY-RUN is now **safe to run unattended** (150.94 GB local hashing, $0, no bandwidth) → next reversible step. Hydration-policy decision (a) is now defaulted-safe; owner input reduces to root-scope + routing. Appended exact scope to APPROVALS ②.

**🆕 2026-07-01 pass — GUARDED SYNC dry-run harness built + verified (reversible/local, $0, NO transfer, NO source writes).**
Serviced the new BOARD 2026-07-01 SORT pickup: the guarded local-drives + OneDrive Case Bible → R2 `casebible-raw` sync. Delivered:
- **`specs/cb_guarded_sync_dryrun.py`** — read-only DRY-RUN planner. Opens every source file `'rb'` only (no write/move/delete path to any source ⇒ `D:\Backup` physically never modified). Enforces all 4 guards: DRY-RUN, junk exclude-list (2026-06-29 software-junk rule), md5 dedup-skip vs `casebible.duckdb→r2_files.md5`, never-modify-`D:\Backup`. Emits ledger: NEW/DUP/EXCLUDED/READ_ERROR/DEFERRED_LARGE + a NEW-bytes transfer estimate.
- **`specs/guarded-sync-PLAN.md`** — the plan (sources, guards table, statuses, next steps, gated items).
- **VERIFIED** (output `specs/guarded-sync-DRYRUN-smoketest.csv`): R2 snapshot loaded = 277,417 distinct md5 (over 590,560 objects). `D:\Backup` bounded sample (400 files, ≤5 MB): **NEW=178 · DUP=222 · DEFERRED_LARGE=51 · READ_ERROR=0**, 9.5 s, Backup untouched → dedup-skip provably works (222 already in raw). status/ sample: 4 NEW (correctly not-in-R2).
- Sources sized (read-only): OneDrive Case Bible = **178,499 files** (size still computing — placeholder stat slow, not hydrated); `D:\Backup` = read-only source.
- **GATED next:** owner confirms root list + OneDrive hydration policy → full DRY-RUN → exact object-count/GB → then the `rclone copy NEW → r2:casebible-raw` on OVH-2 (billable, owner sign-off). Queued as a gatedItem to the orchestrator.

**Prior state:** ✅ ROOT-PILE SORT DONE + VERIFIED. ✅ Cloud-handoff content SUPPLIED.

**🔄 2026-06-26 — back online (fresh session after restart; broken hook plugins disabled, git fixed).** Read whole board; state reconfirmed. Formatter plugin `format.sh` errors on missing `jq` = cosmetic PostToolUse warning only (Write/Edit succeed). Serviced the outstanding cloud-handoff task (TASKS 14:35/14:55): wrote `casebible-coordination/specs/sort-dedupe-handoff-content.md` — the VERBATIM sort/dedupe logic + all 8 owner sections (sources, md5 best-version dedup, copy-only, vault layout, classify() tree, multi-tag metadata, idempotency/ledger, config/env — NO secret values). Handed to PIPELINE in LOG to drop into `goals/sort-dedupe/HANDOFF.md` on branch `agent-handoff/sort-dedupe`.

**✅ ROOT-PILE SORT EXECUTED (11:55):** 400 ledger rows → **364 content COPIED raw→sorted** (type-first; 13 top-level type folders), **35 obvious junk COPIED raw→quarantine/_root_cleanup_2026-06-25/**. raw root INTACT (401, all copies, ZERO deletes, 0 errors). Provenance: `D:/casebible/exports/sort_executed_typefirst_root_20260625.csv`. Existing sorted content UNCHANGED (additive only). 195 needs_review routed+tagged in place for later human refinement. (364-vs-365 = one dup enrichment record copied twice to same path; no loss.)

**🧊 FREEZE (LOCKS.md 10:24) still valid:** existing sorted content was untouched (my write was additive), so PROCESS's AI Chats/ ingest snapshot is unaffected; vault is consistent.

**NEXT (per owner's sequence — all need a go):** (2) ingest AI conversations → Milvus `casebible_ai_conversations` (my collection; embeddings = small $, so $-gated); (3) Evidence.dev dashboard. Also open/optional: refine the 195 needs_review; domain→type re-bucket of the EXISTING ~4,262 sorted files (separate migration); optional raw-root tidy (move now-sorted originals to raw/_moved_2026-06-25/, preserving structure — owner's "moved folder" pattern).

**✅ DRY-RUN PROPOSAL LEDGER — REFINED (pass 3):** `D:/casebible/exports/sort_proposal_typefirst_root_20260625.{parquet,csv}` — 397 root files, type-first, **no R2 ops**. Generator: `tools/cb_typefirst_ledger.py` (draft; canonical cb_r2_sort.py untouched).
- **ROUTE→sorted 362:** Tools&Platform 81 · Knowledge 79 · Evidence 77 · Inbox/triage 47 · Legal 41 · Exports&Bundles 27 · Entities 8 · Documents 2.
- **Evidence by SOURCE:** messaging(fb 13/sms 7/imsg 3/phone 1/misc 23) · records-data 17 · media 4 · screenshots 4 · public-record 3 · financial 1 · audio 1.
- **QUARANTINE 35** (only obvious junk): shader-junk 18 · flagged-junk 13 · installer 4.  ·  needs_review: **195**.
- **Pass-3 rescue:** the model over-flagged junk — rescued ~9 real-content false positives (`data.json`=139pg PDF, `content.zip`=227 PDFs, `place_id_db.json`=geolocation DB, `enriched_timeline_human_readable.csv`, etc.) and kept ~16 of the case's own vault/setup scripts in Tools&Platform. Quarantine dropped 61→35. All rescues flagged needs_review.
- **For human review (parked w/ the eventual R2-transfer approval):** confirm the rescues; 23 unsourced messaging; the Tools&Platform code set (case-tool vs third-party vs keep).

**Sort model (final, from human):** folders = **TYPE-first** (Knowledge · Evidence · Legal · Entities · Case Management · Tools & Platform · Inbox; + Quarantine). Inside **Evidence → by SOURCE/platform** (sms/imessage/facebook/snapchat/phone…). **Domain is NEVER a folder** — it's rich multi-tag metadata + a downstream DB view. AI chats = **Knowledge** (Context Corpus, not evidence). Tag everything early (domain+type+function+source/parties/dates/relevance/dedup).

**Reconciliation DONE (read-only, this pass):** root pile is **396 files**, not 660.
- **396** enriched **and** currently at R2 root → the real sort target.
- **259** enriched rows no longer at root (already moved/quarantined / mirror drift) → no action.
- **5** at root but **un-enriched** → need enrichment first: `README.md`, 2 `.md` docs, 3 export zips (`Snap_Export-1124.zip`, `case chats.zip`, `takeout-…zip`).

**Next reversible/local steps (queued):**
1. Build the **type-first proposal ledger** for the 396 (dry-run parquet + distribution, no R2 ops) for human review.
2. Enrich the 5 un-enriched root files (Ollama, free) — *holding* (DB write; will do on go or fold into the run).
3. Update `cb_r2_sort.py` to the type-first + full-tagging model (pending human go on tool design).

**GATED (in APPROVALS when ready):** the actual root-pile R2 copy + quarantine move (cloud transfer) — needs human go + still paused.

**Sorted-vault state:** UNCHANGED (prior domain-based layout). Not a stable snapshot target yet, but no restructure started — PROCESS may treat current sorted as static for now. I'll post a LOCKS.md freeze window before any write run.

**Open items:** re-bucket existing sorted (domain→type, esp. 3,646 in Evidence/ = messaging); Code & Tools judgment pass (96 candidates); ingest AI convos → Milvus `casebible_ai_conversations`; 349 unresolved reconciliation files (earlier run).

**Idle counter:** 0 — **Phase-2 (1,166) + orphan-cleanup (1,945) both READY, GATED on owner go** (2026-06-27 pass 31: prepped run#1 orphan list (reversible/$0) — only 1,945 true orphans (5,318 run#1 copies are correct/keep); specs/rebucket-run1-ORPHANS-for-cleanup.csv (delete=gated). Holding for owner: (a) "execute phase 2" (1,166 copies), (b) orphan delete approval. — pass 30: Phase-2 suffix-resolver recovered 1,218 (exec-ready 10,549/15,083); 230 ambiguous + 4,304 missing → defer/re-enrich. Phase-2 copyset = 1,166 net-new collision-verified ops (specs/rebucket-PHASE2-copyset.csv). Tried to auto-execute → classifier correctly BLOCKED (transfer needs explicit sign-off; "continue raw→sorted" ≠ blanket transfer approval). HELD for owner "go". Re-bucket v2 already complete (8,475 ok/0 err). — pass 29: corrected collision-free re-run done clean — 8,465→sorted + 10→quarantine, rclone-verified, raw intact. Ledger rebucket_executed_20260627_v2fixed.csv. PIPELINE patched §5 (2fc4b13). Phase-2 = 5,752 unresolved (4,473 basename-ambiguous→suffix-resolvable + 1,279 missing→maybe re-enrich). Cleanup debt: run#1 ~8,917 flat orphans in sorted (never-delete, retire later). Next: build Phase-2 suffix-resolver (reversible/$0)→dry-run→sign-off→copy. AI-chats $-gated/Semantica-held. — pass 28: run #1 (9,331) = 8,917 ok/414 ERR — BUG: basename-only new_path → 563 collision groups/2,632 files (~2,069 overwrote/errored, incl. FB message_1.json evidence). CONTAINED: copy-only, raw 100% intact, zero source loss. FIX: collision-free copy set (basename-if-unique else full raw key) = 8,475 ops, specs/rebucket-COPYSET-fixed.csv → re-running NOW (bg, executed v2 ledger). Run #1 flat copies = orphans (cleanup later). HANDOFF feedback to PIPELINE pending patch. — pass 27: reviewed goals/sort-dedupe/HANDOFF.md (PIPELINE pushed agent-handoff/sort-dedupe) — faithful; flagged 1 DRIFT (§1 still has Legal/* but locked model DROPPED Legal → legal_filing→Case Mgmt, legal_reference→Knowledge) + 3 adds (2-LLM quarantine rule, Tools&Platform substructure, key-drift resolution). Awaiting PIPELINE patch. Re-bucket transfer STILL RUNNING (bg, 20 rclone workers) — verify ok/err on completion. — pass 26: owner "go" → running cb_execute_rebucket.py (bg, 16 workers): 9,331 copies — 9,321→casebible-sorted (type-first) + 10→quarantine. COPY-ONLY/raw-untouched/$0/ledgered (D:/casebible/exports/rebucket_executed_20260627.csv). Additive over old domain layout. Awaiting completion → verify ok/err. Phase-2 (5,752 unresolved) next. — earlier pass 25: 2nd-LLM quarantine pass DONE (gpt-4o-mini, ~$0.04) → only 10 confirmed junk of 522; 512 disputed→Inbox/_rescued_review_2ndllm (190 unresolved $R-recycle defers→human review). FINAL: Evidence 6004·Knowledge 4397·Tools&Platform 1879·Inbox 1635·CaseMgmt 788·Exports 279·Docs 63·Entities 28·Quarantine 10. specs/rebucket-ledger-FINAL.csv. 9,321 non-quarantine resolved = ready to copy on sign-off. Phase-2 (5,752 unresolved) next. — earlier pass 24: owner "do it" + NEW HARD RULE = quarantine needs a 2nd different-LLM pass + human review (memory casebible-quarantine-verification). FIX#1 keys: 9,331/15,083 execution-ready (exact 8020+basename 1311); 5,752→phase-2. FIX#2 rescue: 913 pulled out of quarantine; Quarantine 1435→522, all queued for 2nd-LLM (specs/quarantine-2ndLLM-WORKLIST.csv). specs/rebucket-ledger-REFINED.csv. NEXT: (a) 2nd-LLM quarantine pass on 522; (b) owner sign-off → execute R2 copies for 9,331 resolved non-quarantine (copy-only/ledgered/never-delete); (c) phase-2 resolve 5,752. — earlier pass 23: owner: "continue raw→sorted WITHOUT the full pipeline; revisit pipeline later." Sorting is copy-only/decoupled → Semantica hard-stop doesn't apply; AI-chats EMBED-ingest stays held. Built DRY-RUN re-bucket ledger (catalog, $0): 15,106 enriched → 15,083 canonical (7.8GB) type-first w/ owner refinements (TraceIQ 1042, wiki→Knowledge 380, T&P substructure). specs/rebucket-ledger-{DRYRUN.csv,REVIEW.html} (HTML sent). NOT execution-ready: (1) only 8004/15106 md5-matched → resolve exact RAW keys before copy; (2) Quarantine 1435 mostly model flagged-junk → rescue-pass needed. Next: fix key-join + rescue-pass → owner sign-off → execute copies. — earlier pass 22: owner told SORT directly "AI-chats ingest <$10 just do it + extract uniques + manifest"; I did the dedup (391 unique/9 exact-dup/13 version-clusters, manifest+HTML sent), ran a 3-file pilot, launched full ingest — THEN hit the 04:12 owner hard-stop (Semantica not in workflow). KILLED the full run (0 files written) + DROPPED collection → casebible_ai_conversations EMPTY/clean, nothing persisted under hold. My dedup = forensic EXACT (custody-safe), complementary to Semantica's fuzzy dedup (per ORCH 04:18). HOLDING AI-chats ingest + re-bucket until owner picks Semantica scope (A/B/C). Re-bucket DRY-RUN ledger still drawable (reversible) if wanted. — earlier pass 21: owner present + asked directly what SORT waits on them for. Surfaced the consolidated decision menu (in chat + LOG 03:21): (1) folder-structure rework settled? → re-bucket dry-run; (2) AI-chats ingest $-approval (~$1-3); (3) carve-pile (held till re-bucket); (4) 5 parked items. Awaiting green-light on (1)+(2). Tight ~5min cadence while owner active. — (earlier pass 16: LOW-FREQUENCY HOLDING: substantive SORT work complete this session; last loose end cleared (PIPELINE phantom-dependency, 01:48). No new inbound, no imminent SORT event — all remaining gated/held on owner. Backing off to ~20min to save tokens; will tighten to ~5min the instant a SORT directive/owner decision/freeze request appears. — pass 15: cleared PIPELINE phantom dependency; PIPELINE ACKed Milvus zero-data-loss (consensus). CLARIFIED in LOG: ZERO data loss — the AI-chats ingest was always $-gated/never run, so the collection was empty; "re-ingest" = the same pending FIRST ingest, source AI Chats/ intact + re-embeddable (~$1-3). Could not live-verify Milvus (no pymilvus in venv) — board-derived, offered attu/PIPELINE confirm. May prompt owner to approve the now-clearly-needed ingest → staying responsive. — pass 12: idle, SBV cutover live + Milvus-split. All remaining work gated/held: messaging re-bucket awaits owner's folder-structure rework; carve-pile quarantine queued for when re-bucket ready; AI-chats ingest $-gated. Other lanes active (PROCESS provisional ingest, PIPELINE Option A) but no SORT dependency. Backing off to ~20min to save tokens; will drop to ~5min the moment a SORT directive/owner decision appears. (Done this session: handoff content, sort-rootpile-RESULTS, raw-canonical-formats-RESULTS + carve addendum.)).
**Owns:** `casebible-sorted`/`-raw`/`-quarantine` writes, ovh2 enrichment PG, sort ledger, Milvus `casebible_ai_conversations`.
**Will NOT touch:** agno stack / platform repo / ingestion collections (`casebible_evidence`, Neo4j, OVH-3).
