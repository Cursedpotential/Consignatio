# R2 to B2 migration status — 2026-09-12

## 2026-09-13 07:xx UTC best-copy metadata reconciliation in progress

- The exact-copy rule is now field-level: the healthy occurrence with the richest
  trustworthy metadata is the main representation even when it has a damaged
  recovery name; the canonical display filename is repaired/selected independently.
  Every observed name, date, provider field, sidecar value, and source path remains
  provenance. Unresolved values remain side by side.
- The migration builder implementation is committed on
  `codex/r2-b2-best-copy-20260913` at `eca84bd`. It adds append-only metadata
  assertions/resolutions, independent payload/name/date donors, sentinel-date
  rejection, complete assertion exports, and a hard block on conflicting embedded
  metadata inside a claimed exact-byte group.
- The existing databases are now inputs. Nine PG18 metadata shards cover R2,
  OneDrive, Google Drive, local files, atomic paths, legacy hashes, recovery links,
  media metadata, and derived dates. The D: DuckDB/SQLite catalogs, historical
  pre-merge `E:/e.efu`, and the owner's current OneDrive desktop export are attached
  read-only. D is now the consolidated local corpus; E is mostly empty after its
  merge into D.
- The SHA metadata-only export completed at 256/256 partitions, 256 receipts, zero
  partials, and 338,318 records. The R2-to-B2 payload copy remains stopped.
- A standalone DuckDB selection attempt was rejected because it counted several
  attached sources without importing their field assertions and could promote a
  historical path-plus-size match to exact identity. Its partial database and WAL
  were preserved in `to_be_deleted`; they are not an accepted manifest. The
  corrected path uses the tested ledger resolver as the single selection engine.
- Fresh D:, post-merge E residual, F:, J:, OneDrive, and four Google Drive passes
  remain required source by source. OneDrive content lookup defaults to R2; remote
  provider checks are metadata-only and must not hydrate local placeholders.
  Historical inventories are baselines, not September freshness proof.
- See `BEST-COPY-METADATA-RECONCILIATION-2026-09-13.md` for current receipts,
  source counts, exact owner contract, and remaining gates.

## 2026-09-13 15:40 UTC R2-retirement blocker — zero-filled payloads (read-only finding)

> _Byline: Claude Code · Opus 5 · 2026-09-13_

- **Do not retire or delete R2 yet.** B2 currently holds zero-filled copies whose
  only good bytes are still in R2.
- Method: listing and inventory only; no object bytes were downloaded for this
  check. For every size in `r2_files` (MD5) and in a fresh `b2:salem-data` listing
  (SHA-1), the hash of an all-zero payload of that size was computed and matched.
- R2 (`casebible.duckdb` `r2_files`, 1,279,556 rows): **20,512 all-zero files
  (20.12 GB)**. 18,882 have a good copy in R2 (same name and size, non-zero bytes).
  1,630 do not; 1,507 of those are `photo_recovery` carvings.
- B2 (103,076 objects, 262.04 GB): **6,362 all-zero objects (7.1 GB)**, 6,315 under
  `casebible-raw/_backup_import`. 1,486 have a good same-name copy on B2. Of the
  other 4,876, 4,866 have good content in R2, and every one of those MD5s is in
  `final_survivors`. 313 of those survivors are already on B2 under another path.
  **4,553 are not yet on B2**: their survivor rows are in `casebible-quarantine`
  (tranche not started) or the unfinished `casebible-raw` tranche.
- OneDrive listing (`od_case_bible.tsv`) against R2 found no file whose R2 copies
  are all under half of the OneDrive size.
- Google Drive `gd_salemnet` (metadata listing only) has 166 zero-filled files.
  It holds good copies for 1,021 R2 zeroed files, and for 9 of the 10 B2 zeroed
  files that have no good copy in R2 or B2. The one file with no good copy
  anywhere is `Google Data Export Archive Contents (0015A63D).html`.
- Root-cause lead: local `D:\Backup` holds zero-filled files whose MD5s match
  the Google Drive `99_Archive` zero copies. The zero bytes spread from that
  source into `_backup_import` and Google Drive. Local scans of D:\Backup, F: and
  J: are running; results go in the receipt folder `dbackup/`.
- Resume and retirement gate: after transfer, re-run the zero-hash check on B2 and
  confirm each of the 4,553 good payloads is present by hash before any R2
  retirement. Zero-filled copies are corrupt occurrences; record them and never
  select them as the best copy.
- **Quarantine applied 2026-09-13, owner-approved and verified by read-back:**
  1. PG `casebible.raw_duck` on ovh-files (container `fgz1n7useplhk0t91uk7k1aw`):
     new audit table `raw_duck.integrity_hold` with 33,685 rows, each keeping its
     original hash. Added `integrity_status` and `integrity_reason` columns to
     `r2_files` and `final_survivors`. Flagged 24,214 `r2_files` rows (20,512
     zero-filled, 3,702 zero-byte) and excluded 9,471 `final_survivors` (9,470
     zero-filled, 1 zero-byte); the hash is nulled on all of them, 0 are still
     hashed, and 0 good rows were touched. 327,596 survivors (1,105.1 GB) remain
     eligible.
  2. Local `casebible/casebible.duckdb`: the same change, identical counts.
  3. VPS `pg18-survivor-copy-20260912/casebible-raw.files-from-raw` filtered from
     188,804 to 182,442 lines (6,362 zero-filled keys removed, 0 left). The
     original is kept byte-identical in `superseded-20260913/`. Hold manifest
     published to `controls/zero-filled-hold-20260913.csv` (27,040 rows, sha256
     `bfc0a8a4e112c6e70f4ce298e6bb6f3156138ac8d1dfea9c80d0e021948af151`) with a
     README. The older `controls/ENABLE_CORPUS_TRANSFER.20260912.approved` file
     is untouched; the resume block above still governs.
  4. B2 objects moved 2026-09-13 12:02 EDT with server-side `rclone move`: 6,362
     zero-filled objects from `consignatio/intake/raw-dedupe/v1/` to
     `consignatio/intake/_quarantine/zero-filled/v1/` (same relative paths),
     0 errors. Verified PASS from fresh listings: 0 missing, 0 size mismatches,
     every SHA-1 is the all-zero hash, 0 keys left at the source. The raw-dedupe
     prefix now holds 96,633 objects. The B2 source keys were hidden, not
     hard-deleted.
- **SHA-256 ledger coverage, measured 2026-09-13 13:20 EDT** (read-only, tracked
  `casebible/tools/sha_ledger_coverage.sh`, rolled back): 338,318 exported ledger
  records, 323,781 distinct SHA-256, no multipart ETags. Of 327,596 distinct
  eligible contents, 314,312 (95.9%) have a SHA-256 and 13,284 are MD5-only
  (including 459 of the 460 large `_deferred` objects). The only content with no
  hash is `casebible-sorted/Triage/exports-bundles/misc/Katrina-1-001.zip` (7.14 GB).
  `corpus-ledger.sqlite` imported only 79 of the 256 partitions (104,412
  bindings), so it is incomplete as a dedupe authority until the remaining
  partitions are imported.
- **Resume gate added:** any resume, manifest build or grading run must load
  `controls/zero-filled-hold-20260913.csv` and exclude every row. The manifest
  builder reads `--ledger` and `--inventory` files rather than live `raw_duck`,
  so the hold must be applied to those inputs too.
- Evidence files: ~~`E:/AI_Workspace/_receipts/corruption-hunt/`~~ `Consignatio/docs/receipts/corruption-hunt/` (moved into the repo 2026-09-15)
  (`r2_all_zero_files.tsv`, `r2_zeroed_recoverability.csv`,
  `b2_all_zero_objects.tsv`, `b2_zeroed_good_copy_locations.csv`,
  `b2_zeroed_survivor_status.csv`, B2 listing TSV).

## 2026-09-13 06:xx UTC owner-corrected pause — best-copy grading absent

- The owner identified that exact-byte identity does not imply identical or
  equally valuable filesystem/provider metadata. Duplicate occurrences can
  have different created/modified times, paths, names, provider IDs,
  versions, sidecars, and contextual placement.
- The running v2 transfer used `raw_duck.final_survivors` and copied with
  `--metadata`, but its representative rule is not the previously discussed
  best-copy grading contract. The tracked builder explicitly records
  `selection_is_quality_judgment = 0` and selects a planning representative by
  latest inventory timestamp plus ordinal path order. That can preserve the
  metadata of a later copied/exported occurrence instead of the occurrence
  with the strongest original metadata.
- The transient unit
  `consignatio-pg18-r2-to-b2-raw-dedupe-20260912-v2.service` was therefore
  stopped before more B2 space was consumed under the incomplete selection
  rule. Read-back showed `inactive` / `dead`, `Result=success`,
  `ExecMainStatus=0`, and no matching rclone process.
- The pause is reversible. No R2 source, B2 object, occurrence record, manifest,
  or provenance record was deleted, moved, or overwritten. Already-copied B2
  objects remain raw staging material and receive no canonical, sorted, Vault,
  evidence, or custody status.
- ~~The owner restated the best-copy grading precedence for exact-content
  occurrences: first preserve the original/native filename; next prefer the
  oldest trustworthy date; next prefer the occurrence with the most complete
  date set; then prefer the most complete metadata overall.~~ **Corrected
  2026-09-13 15:46 UTC by the owner (recorded by Claude Code · Opus 5):** prefer
  the most accurate metadata, first the oldest trustworthy date and then the
  most complete date and metadata set. The filename is the *least* important
  component. If no winner can be chosen, keep both files. Byte/content identity
  is a hard prerequisite, not part of the quality score. All losing occurrences
  and their metadata remain in the occurrence map. ~~Ambiguous ties and
  contradictory date/metadata assertions remain review holds.~~ Ties are no
  longer held for review; keep both copies.
- Zero-filled and zero-byte occurrences are never candidates: their hash is
  nulled in the catalog and they are excluded before grading (see the
  2026-09-13 R2-retirement blocker above).
- Resume is blocked on implementing that grading contract, comparing its
  selected occurrences with `raw_duck.final_survivors`, preserving alternate
  representations as separate content, recording every score component and
  reason, and passing a manifest/read-back gate. Do not resume from the
  existing ungraded `*.files-from-raw` lists.

## 2026-09-13 06:20 UTC live transfer checkpoint

- The corrected server-to-server unit
  `consignatio-pg18-r2-to-b2-raw-dedupe-20260912-v2.service` remains
  `active` / `running`; its live process is copying `casebible-raw` directly
  from R2 to B2.
- The latest rclone stats line at 06:19:58 UTC reported 241.427 GiB of a
  still-growing 275.754 GiB discovery total (88%), 101,784 completed
  transfers out of 111,790 then discovered, and an estimated 1h41m39s. The
  denominator and ETA remain discovery-dependent and are not completion
  promises.
- The log contained 101,967 `Copied (new)` entries. A precise rclone error
  pattern found zero error lines and zero failed-copy notices. A naive
  case-insensitive search for `ERROR` produced 54 false positives from source
  filenames such as `error_context.md`; those are successful copy records,
  not transfer errors.
- Current memory was 344,543,232 bytes and peak memory was 464,908,288 bytes,
  below the 1 GiB unit ceiling.
- `casebible-sorted` remains complete. `casebible-quarantine` has not started.
- Destination semantics remain
  `consignatio/intake/raw-dedupe/v1/source-buckets/<source-bucket>`.
  This job performs immutable physical copy/dedupe only. It does not delete
  sources, classify content, infer atomic units, accept Vault items, create
  evidence, or begin custody.

## 2026-09-13 03:55 UTC Git and destination-contract reconciliation

- The source, tests, and current operating documentation are now isolated on
  `codex/r2-b2-migration-reconcile-20260913` in a linked checkout at
  `E:/AI_Workspace/Projects/Propria/_worktrees/consignatio-r2-b2-reconcile`.
  The owning repository remains Consignatio, and the tracked source path remains
  `casebible/r2-b2-migration-codex/` inside that repository.
- Executable defaults and guards now use only
  `consignatio/intake/raw-dedupe/v1/`. The superseded
  `consignatio/vault/v1/` prefix is retained below as historical incident
  evidence and in negative tests that prove the runtime rejects it.
- Generated PG18 manifests, temporary test state, the operator enable control,
  and safe-operation backups remain preserved locally and are explicitly
  ignored. They are not source and are not included in the Git reconciliation.
- Read-only live verification at 03:55 UTC found
  `consignatio-pg18-r2-to-b2-raw-dedupe-20260912-v2.service` active/running.
  Its actual `ExecStart` targets
  `b2:salem-data/consignatio/intake/raw-dedupe/v1/source-buckets/$bucket`, uses
  immutable copy, and was processing `casebible-raw`. The log contained 61,040
  `Copied (new)` entries and zero `ERROR` entries at that checkpoint.
- The corrected source has not been deployed over the historical runtime. The
  active v2 copy is a separately created transient systemd unit. No service,
  B2 object, operator gate, or remote runtime was mutated during Git
  reconciliation.

See `RECONCILIATION-RECEIPT-2026-09-13.md` for exact Git and validation proof.

## 2026-09-13 03:40 UTC live transfer checkpoint

- Active unit: `consignatio-pg18-r2-to-b2-raw-dedupe-20260912-v2.service` (`active` / `running`).
- Correct destination remains `b2:salem-data/consignatio/intake/raw-dedupe/v1/source-buckets/`; source bucket/path is provenance only.
- `casebible-sorted` historical source-bucket tranche completed at 21:11:57 UTC. This does not confer sorted, reviewed, accepted, or Vault status.
- `casebible-raw` historical source-bucket tranche is in progress. Latest recorded stats at 03:39:58 UTC were 189.258 GiB transferred in the current rclone invocation and 56,586 completed transfers; discovery was still increasing the reported denominator.
- Log proof at this checkpoint: 56,877 `Copied (new)` entries and zero `ERROR` entries.
- Runtime stayed inside its bound: 374,624,256 bytes current memory and 464,908,288 bytes peak versus the 1 GiB systemd ceiling.
- The `casebible-quarantine` historical source-bucket tranche has not started yet.
- This is physical exact-identity dedupe/copy only. It performs no sorting, classification, atomic-unit inference, evidence acceptance, or source deletion.

## 2026-09-12 21:xx UTC authoritative correction and stopped state

- **Correct destination semantics:** `b2:salem-data/consignatio/intake/raw-dedupe/v1/`. This is Intake raw dedupe, not the Vault.
- Original R2 bucket and object path may be retained only as source provenance. Names such as `casebible-sorted`, `casebible-raw`, and `casebible-quarantine` do not assert reviewed, sorted, accepted, classified, or Vault status.
- `raw_duck.final_survivors` is an old one-row-per-MD5 physical-copy selection. It does not confer semantic authority or canonical evidence status.
- This transfer stage performs exact-byte physical deduplication only. It must not classify, rename, reorganize, infer atomic units, or make human-review decisions. Atomic-unit detection and assisted grouping belong to Xplorer/Intake.
- The corrected transient unit `consignatio-pg18-r2-to-b2-raw-dedupe-20260912.service` was stopped and verified `inactive` / `dead`. No matching copy process remains.
- The corrected attempt completed no files. Its log reported B2 `401 unauthorized`; the only recorded progress line showed `xfr#0` before stop.
- Root cause: the installed B2 application key is write-capable but restricted by B2 to the filename prefix `consignatio/vault/v1/`. It cannot write the corrected `consignatio/intake/raw-dedupe/v1/` namespace.
- The corrected Intake destination prefix was checked and was empty. Do not restart until an existing or newly provisioned B2 key explicitly permits the Intake prefix (or a safe broader prefix).
- No objects were deleted or source-mutated. Earlier material written beneath the wrong Vault prefix is an erroneous projection and must not be treated as reviewed Vault content.

> The sections below preserve the earlier execution record. Any statement below that calls `consignatio/vault/v1/` the canonical destination, calls a transfer active, or treats `final_survivors` as semantic selection is superseded by the correction above.

## 2026-09-12 20:xx UTC corrective stop

- All four transient migration services were stopped and verified `inactive`: the two-reader SHA-ledger packaging export, the waiting live-inventory job, the manifest finalizer, and the transfer follow-on.
- `systemctl is-enabled` returned `not-found` for all four after stopping; they were transient units and cannot restart automatically.
- The Codex heartbeat `monitor-consignatio-r2-to-b2-migration` was deleted so it cannot continue background polling.
- No corpus payload had been transferred to B2. The destination contained only the 114-byte restricted-key canary.
- The immediate cause was a wrong architectural assumption: this lane treated migration-manifest packaging as prerequisite SHA work without first honoring the canonical `casebible/HANDOFF.md`, which states that the corpus SHA-256 backfill was already completed on 2026-08-17 and that its records already carry the MD5-to-SHA-256 bridge.
- No replacement importer, ledger, fast-track script, or alternate transfer service was created or deployed after this was identified. Recovery must begin with read-only discovery of the existing PG18 inventory/hash implementation and its established transfer entry point.

## 2026-09-12 20:49 UTC actual server-to-server transfer

- Canonical inventory source: Case Bible PG18 on `ovh-files`, table `raw_duck.final_survivors`, joined conceptually to the preserved `raw_duck.r2_files` occurrence inventory and the existing R2 SHA-256 ledger.
- PG18 source facts: 337,067 distinct MD5 identities selected from 1,279,556 R2 occurrences; 337,066 positive-size payloads totaling 1,114,709,222,228 bytes were queued; one zero-byte row was held.
- The selection is used only to choose one physical payload per exact-byte MD5 identity. PG18 retains all occurrences, including corroborating copies, alternate paths, exports, and recovery locations.
- The first real corpus payload was copied from R2 to B2 and independently read back from B2. Source size and MD5 matched the existing ledger; destination size was 144,720 bytes and destination SHA-256 matched `1f900c83d45b6b2b7da57974a7f476f4f4de57b28e531dc8ee1822ad98becf35`.
- Active transient unit: `consignatio-pg18-r2-to-b2-copy-20260912.service`.
- Transfer path is server-to-server: R2 -> bounded rclone process on `ovh-files` -> B2. The Windows workstation is not a corpus-data hop.
- Transfer order is `casebible-sorted`, `casebible-raw`, then `casebible-quarantine`, with two transfers, four checkers, immutable destination writes, metadata preservation, no destination traversal, a 1 GiB memory ceiling, and no source deletion.
- Live log: `/data/consignatio/migrations/r2-to-b2/pg18-survivor-copy-20260912/transfer.log`.
- Confirmed live copies include real messaging ZIP/CSV/PDF files, DOCX/Markdown/XLSX documents, Facebook media, recovery files, and the existing `__r2_data_catalog` Parquet/Avro/metadata objects.

## Canonical scope

- Source corpus: all occurrences recorded across the three legacy R2 buckets.
- Destination: `b2:salem-data/consignatio/vault/v1/`.
- Physical payload deduplication is authorized only by verified SHA-256 identity or the frozen, conflict-free `(MD5, byte size) -> SHA-256` bridge.
- Every source occurrence and its original bucket/path remains in the lake catalog even when many occurrences share one physical payload.
- Legacy path labels such as `junk`, `quarantine`, `dupe`, `to_be_deleted`, and `.review_hold` are untrusted metadata and never migration exclusions.
- Alternate formats, exports, screenshots, and independently recovered copies remain distinct corroborating occurrences unless byte identity is actually proven.

## Verified baseline

- PG18 inventory: 1,279,556 occurrences across `casebible-raw`, `casebible-sorted`, and `casebible-quarantine`.
- Inventory SHA-256: `41dee727a3737d634420acc0202abfc29dcb7c93504de854d9a0afaec1173ff0`.
- Valid `(MD5, byte size)` groups: 337,067, totaling 1,114,709,222,228 bytes (about 1.01 TiB).
- Rows without a usable inventory MD5: 79, totaling 274,247,900,317 bytes. These remain held for conservative source-identity handling and are not silently discarded.
- Restricted B2 canary passed; the B2 key cannot delete objects and is scoped to `consignatio/vault/v1/`.

## Active VPS jobs

### Critical clarification: the corpus SHA-256 work already exists

- The prior worker's completed SHA-256 authority has **not** been lost or replaced. It is stored in the dedicated R2 ledger at `r2:casebible-hash-ledger/sha256/v2` and contains the expected 338,318 SHA records.
- `export_sha256_partitions.sh` does **not** hash the corpus payloads again. It streams those existing JSON ledger records into 256 bounded, gzip-compressed partitions, validates their schema/digests, and writes per-partition counts and SHA-256 sidecars.
- As of 2026-09-12 20:27 UTC, 243 of 256 packaging partitions were finalized and prefixes `f3` and `f4` were in flight.
- The B2 destination contained one 114-byte restricted-key canary and **zero corpus payloads**. The transfer follow-on was waiting behind partition packaging, fresh live inventory, manifest construction, dry runs, and the first fully verified copy.
- Operational status must distinguish **existing corpus hashes**, **migration-manifest packaging**, and **actual B2 payload transfer**. Reporting the packaging job merely as "SHA export" obscures that the expensive hashing work was already completed and that no corpus transfer had begun.

- `consignatio-r2-sha256-partitions-2w-20260912t183000z.service`
  - Streams 256 R2 SHA-ledger prefixes into independently checksummed NDJSON-gzip partitions.
  - Fixed at two readers to preserve Windows `V:`/`Y:` mount responsiveness.
  - Existing finalized partitions are immutable; interrupted partials are moved to the migration `to_be_deleted` area before resume.
- `consignatio-r2-live-inventory-after-sha-20260912t175000z.service`
  - Waits for the SHA readers to finish, then inventories one live R2 bucket at a time.
  - Emits quoted `pshtm` CSV-gzip inventories with row/byte receipts and checksums.
  - This closes the approximately 161-path drift between the PG18 snapshot and live R2 without running concurrent full-corpus listings against the local mounts.
- `consignatio-r2-b2-live-manifest-finalize-20260912t180000z.service`
  - Waits for exactly 256 partitions, 338,318 nonblank hash records, and the complete fresh live inventory.
  - Repairs historical separator counts without deleting their audit assertions.
  - Builds a fresh live-R2 ledger, freezes the compact SHA bridge, emits the payload and exhaustive occurrence maps, hashes the generation, and runs a full mapping dry-run.
  - Cannot create the corpus enable gate and cannot transfer payloads.
- `consignatio-r2-b2-live-transfer-followon-20260912t180000z.service`
  - Waits for the frozen live generation and verifies all published checksums.
  - Checks that the generation occurrence count exactly matches the fresh live-inventory summary.
  - Installs the separately staged operator-approved corpus gate only after those checks, performs one small full-verification copy, publishes the catalog snapshot, and then starts the two-worker immutable bulk copy.

## Code and validation

- Manifest builder: 12/12 tests pass locally and on the VPS.
- Payload runtime: 24/24 tests pass locally and on the VPS.
- Exporter validates gzip integrity, JSON schema/digest shape, record counts, and SHA-256 sidecars before a partition is accepted.
- Payload runtime uses an isolated Unix-domain rclone RC socket, immutable `copyfile`, bounded workers/memory, append-only outcome ledgers, and separate `copied_unverified` versus `verified` states.
- No R2 inventory listing job is currently competing with the local mounts.

## Remaining execution gates

1. Complete and verify all 256 hash partitions.
2. Complete bridge finalization and generation dry-run.
3. Inspect generated held reasons and byte totals, especially the 79 currently unhashable rows.
4. Split the generated mapping into canonical, source-identity, and held manifests. Fast mode fails before daemon creation if a source-identity row is mixed into the canonical manifest.
5. Run one small real canonical copy with full source/destination SHA-256 verification.
6. Install the corpus gate from the operator-approved control file, run canonical payloads through the bounded two-worker fast path, and run positive-size noncanonical source-identity payloads separately through mandatory one-worker full verification.
7. Publish the frozen generation, exhaustive occurrence map, checksums, and catalog snapshot into the B2 `_system/lake/` prefix.
8. Produce Parquet/Lance artifacts through the shared platform tool runtime. Verified live boundary:
   - `probata-tool-runtime:latest` is healthy on `ovh-app`, behind the governed `tool-gateway`.
   - Its 43-tool registry currently contains no DuckDB, Parquet, Lance, or general conversion capability, and the runtime image has none of `duckdb`, `pyarrow`, `lancedb`, or `pandas`.
   - It cannot see the `ovh-files` migration path and has no scoped B2 writer; the gateway response ceiling also rules out returning large artifacts inline.
   - Add atomic Parquet and Lance exporter tools to the canonical runtime image, plus content-addressed result staging and a scoped B2 publisher. The tool result should be a locator, SHA-256, byte size, and receipt—not the artifact body.
   - The file VPS currently has neither PyArrow nor DuckDB, so package installation on that host is intentionally avoided.

## Related boundary

The Intake graph lane consumes the frozen occurrence-content map in hash-bound batches. It remains read-only with respect to transfer and B2, preserves every occurrence, and creates strong content edges only from the finalized SHA authority.
